# Session Handoff — 2026-05-14

## Project
pops-claw | v2.12 Research Pipeline Modernization | between phases (59 planned, 60-61 contexts in place)
Branch: `main` (clean, in sync with origin)

## What Got Done
- **Diagnosed and fixed tailscaled SIGABRT loop on EC2.** `WatchdogSec=60` drop-in at `/etc/systemd/system/tailscaled.service.d/watchdog.conf` was killing tailscaled every 60s (~1440 restarts/day) because Tailscale 1.96.4 doesn't emit `sd_notify WATCHDOG=1` pings. Each restart flushed a stale logtail backlog (entries from 2026-02-12) to syslog, bloating `/var/log/syslog` to 1.2GB.
- **Applied fix on EC2:** removed `watchdog.conf` (backup at `~/tailscaled-watchdog.conf.bak`), kept `oom.conf` (`OOMScoreAdjust=-900`), `daemon-reload`, manually rotated syslog (logrotate's size threshold didn't fire).
- **Verified ssh/sshd watchdog config** — `ssh.service.d/watchdog.conf` only sets `Restart=always` (no `WatchdogSec`, effective `WatchdogUSec=0`), `sshd.service` has no drop-in. Both safe.
- **Documented the fix** at `docs/solutions/runtime-errors/tailscaled-watchdog-sigabrt-flood.md` (full forensic record) and `~/.claude/projects/-Users-andykaufman-Desktop-Projects-pops-claw/memory/lesson_tailscaled_watchdog_flood.md` (terse rule for future sessions). Corrected misleading MEMORY.md line ("watchdog overrides on both" → flagged as removed and harmful).
- **Cleaned up 3 months of working-tree drift** via PR #1: gitignored `.claude/` + `.DS_Store` + `scripts/*.log` + `yolo-dev/`, archived phase 58 plans, added 7 phase dirs (01, 43-46, 60-61), the long-MIA `59-CONTEXT.md` (weekly-pulse's recurring "MISSING" flag), 22 research briefs, the sync script + plist, the prior docker-mount solution doc, superpowers spec.
- **Net infra wins:** EC2 disk 63% → 59% (~1.5GB reclaimed), available memory 652Mi → 768Mi, tailscaled stable on same PID instead of dying every minute.

## Decisions Made
- **No-push-to-main policy is now active** (added to global CLAUDE.md mid-session). Today's docs commit went directly to main (before policy); subsequent cleanup work used a feature branch + PR. Going forward: all changes to this repo go through a PR.
- **Kept `OOMScoreAdjust=-900`** on tailscaled. Only removed the watchdog override — that's the real protection.
- **Deleted screenshots** `agents-phase46.png` + `growth-login.png` (stale, no doc references). Gitignored `yolo-dev/` rather than committing the experimental `chronicle.py`.

## Files Changed
All committed and merged. Net session deltas on main:
- `+e124b1d` docs (tailscaled fix solution doc)
- `+447a23f` gitignore + junk delete
- `+1d4bae9` planning state catchup (+2701/-2827 lines, 27 files)
- `+dc78f42` docs/scripts/briefs (+1258/-37 lines, 29 files)
- `+0f1d919` merge commit for PR #1

Working tree: clean.

## Next Session Punch List
1. `/gsd:execute-phase 59` — Phase 59 (Research Foundation) is planned and ready; CONTEXT + PLAN both committed. Goal: fix EC2 brief generation, add `brief_generated` flag, set up GitHub as canonical brief store.
2. Verify tailscaled stability 24h post-fix: `ssh ubuntu@100.72.143.9 'sudo journalctl -u tailscaled --since "24 hours ago" | grep -c "Started tailscaled"'` — expect 0 or 1, not 800+.
3. Decide on the pre-existing UTF-8 corruption in `.planning/PROJECT.md` v2.11 description block (`via gbrain ��`) — leave alone or one-line fix-up commit.
4. Optional: write a `/ops` enhancement that alerts when `tailscaled` restart count >5/day, per the prevention section in the new solution doc.
