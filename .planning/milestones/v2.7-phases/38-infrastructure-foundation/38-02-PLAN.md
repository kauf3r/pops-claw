---
phase: 38-infrastructure-foundation
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - ~/clawd/yolo-dev/000-test/README.md
autonomous: false
requirements: [INFRA-03]

must_haves:
  truths:
    - "Bob can read and write to /workspace/yolo-dev/yolo.db from inside the Docker sandbox"
    - "Bob can create a numbered build directory (000-test/) with a README.md inside it from within the sandbox"
    - "Files created in the sandbox at /workspace/yolo-dev/ appear on the host at ~/clawd/yolo-dev/"
  artifacts:
    - path: "~/clawd/yolo-dev/000-test/README.md"
      provides: "Smoke test marker proving sandbox write access to yolo-dev directory"
      contains: "Smoke Test"
  key_links:
    - from: "/workspace/yolo-dev/ (sandbox)"
      to: "~/clawd/yolo-dev/ (host)"
      via: "Docker bind-mount"
      pattern: "000-test/README.md exists on both sides"
---

<objective>
Validate the full infrastructure end-to-end from inside Bob's Docker sandbox: verify the bind-mount works, yolo.db is readable/writable, and Bob can create numbered build directories.

Purpose: Prove that the storage layer actually works from Bob's perspective before Phase 39 builds on it.
Output: 000-test/ smoke test directory with README.md, validated DB access from sandbox.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-infrastructure-foundation/38-CONTEXT.md
@.planning/phases/38-infrastructure-foundation/38-RESEARCH.md
@.planning/phases/38-infrastructure-foundation/38-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate bind-mount and database access from host side</name>
  <files>~/clawd/yolo-dev/yolo.db</files>
  <action>
SSH to EC2 (100.72.143.9) as ubuntu. Run the validation sequence layer by layer (per user decision on validation order):

1. **Service status check:**
   ```
   systemctl --user status openclaw-gateway | grep "Active:"
   ```
   Expected: "Active: active (running)"

2. **Host directory check:**
   ```
   ls -la ~/clawd/yolo-dev/
   ```
   Expected: yolo.db exists, ubuntu:ubuntu ownership

3. **DB schema check:**
   ```
   sqlite3 ~/clawd/yolo-dev/yolo.db ".schema builds"
   ```
   Expected: Full CREATE TABLE with all 17 columns

4. **DB write/read/delete test from host:**
   ```
   sqlite3 ~/clawd/yolo-dev/yolo.db "INSERT INTO builds (date, slug, name, status) VALUES ('2026-02-24', 'validation-test', 'Validation Test', 'idea'); SELECT id, slug, status FROM builds WHERE slug='validation-test'; DELETE FROM builds WHERE slug='validation-test';"
   ```
   Expected: Row inserted with auto-increment id, displayed, then deleted

5. **Verify bind-mount is in Docker config:**
   ```
   jq '.agents.defaults.sandbox.docker.binds' ~/.openclaw/openclaw.json
   ```
   Expected: Array includes "yolo-dev" entry with ":rw"

If any step fails, stop and diagnose before proceeding to Task 2. Common failures:
- Service not running: check `journalctl --user -u openclaw-gateway --no-pager -n 20` for errors
- Permission denied: check `ls -la ~/clawd/yolo-dev/` ownership is ubuntu:ubuntu (not root)
- DB not found: ensure Plan 01 completed successfully
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "systemctl --user is-active openclaw-gateway && sqlite3 ~/clawd/yolo-dev/yolo.db 'SELECT count(*) FROM builds' && jq '.agents.defaults.sandbox.docker.binds | map(select(contains(\"yolo\")))' ~/.openclaw/openclaw.json"</automated>
    <manual>Service is active, DB query returns 0 (empty table), bind config shows yolo-dev entry</manual>
  </verify>
  <done>All 5 host-side validation checks pass: service running, directory exists, schema correct, DB read/write works, bind-mount configured</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bob creates 000-test/ smoke test from inside sandbox</name>
  <files>~/clawd/yolo-dev/000-test/README.md</files>
  <action>
**What was built:** The full YOLO Dev infrastructure: yolo.db database with builds table, ~/clawd/yolo-dev/ directory bind-mounted into the Docker sandbox at /workspace/yolo-dev/ with read-write access.

This checkpoint requires Andy to instruct Bob (running inside the Docker sandbox) to create the 000-test/ smoke test directory, proving end-to-end sandbox write access works.

**Steps for Andy:**

1. Send Bob a Slack DM (or use the OpenClaw dashboard) with this instruction:

   "Create the YOLO Dev smoke test: run these commands and report what happens:
   1. `ls /workspace/yolo-dev/` -- confirm the directory is mounted
   2. `sqlite3 /workspace/yolo-dev/yolo.db '.tables'` -- confirm DB is accessible (ignore any version mismatch warning)
   3. `mkdir -p /workspace/yolo-dev/000-test`
   4. Write a file at /workspace/yolo-dev/000-test/README.md with content: `# YOLO Dev Smoke Test\n\nCreated by Bob on 2026-02-24 to validate sandbox write access.\n\nStatus: PASS`
   5. `ls -la /workspace/yolo-dev/000-test/`
   6. `sqlite3 /workspace/yolo-dev/yolo.db \"SELECT count(*) FROM builds\"` -- confirm DB read works"

2. After Bob confirms success, verify on the host:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cat ~/clawd/yolo-dev/000-test/README.md && ls -la ~/clawd/yolo-dev/000-test/"
   ```
   Expected: README.md content visible on host, proving sandbox writes propagate through the bind-mount.

3. Confirm the 000-test/ directory persists (it's a permanent smoke test marker, per user decision).

**Resume signal:** Type "approved" if Bob successfully created 000-test/README.md and it's visible on the host, or describe any issues encountered.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cat ~/clawd/yolo-dev/000-test/README.md && ls -la ~/clawd/yolo-dev/000-test/"</automated>
    <manual>README.md content visible on host at ~/clawd/yolo-dev/000-test/README.md, created by Bob from inside the sandbox</manual>
  </verify>
  <done>Bob created 000-test/README.md from inside the Docker sandbox, file is visible on the host at ~/clawd/yolo-dev/000-test/, proving the bind-mount provides full read-write sandbox access to the yolo-dev directory</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Gateway is active (running) -- confirmed in Task 1
2. yolo.db is accessible from host with correct schema -- confirmed in Task 1
3. Bob created 000-test/README.md from inside sandbox -- confirmed in Task 2 (human-verified)
4. 000-test/README.md is visible on host at ~/clawd/yolo-dev/000-test/README.md -- confirmed in Task 2
5. All three INFRA requirements are satisfied:
   - INFRA-01: yolo.db has builds table with all columns (Task 1 schema check)
   - INFRA-02: bind-mount configured and working (Task 1 config check + Task 2 sandbox test)
   - INFRA-03: Bob can create numbered build directory with README.md (Task 2 smoke test)
</verification>

<success_criteria>
- Bob successfully ran commands inside sandbox targeting /workspace/yolo-dev/
- 000-test/README.md exists on host at ~/clawd/yolo-dev/000-test/README.md
- yolo.db is readable from inside the sandbox (sqlite3 .tables returns "builds")
- Build directory creation (mkdir) works from inside the sandbox
</success_criteria>

<output>
After completion, create `.planning/phases/38-infrastructure-foundation/38-02-SUMMARY.md`
</output>
