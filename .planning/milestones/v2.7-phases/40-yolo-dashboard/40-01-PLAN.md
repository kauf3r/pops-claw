---
phase: 40-yolo-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db-paths.ts
  - src/lib/queries/yolo.ts
  - src/app/api/yolo/builds/route.ts
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "GET /api/yolo/builds returns JSON with builds array and counts object"
    - "Builds are sorted newest-first by date"
    - "Each build includes id, date, slug, name, description, status, techStack (parsed array), selfScore, linesOfCode, filesCreated, startedAt, completedAt, durationSeconds"
    - "Status counts (total, success, partial, failed) are computed and returned"
    - "yolo.db is registered in db-paths.ts and accessible via getDb('yolo')"
  artifacts:
    - path: "src/lib/db-paths.ts"
      provides: "yolo entry in DB_NAMES, DB_PATHS, DB_LABELS"
      contains: "yolo"
    - path: "src/lib/queries/yolo.ts"
      provides: "getYoloBuilds() function, YoloBuild and YoloBuildSummary interfaces"
      exports: ["getYoloBuilds", "YoloBuild", "YoloBuildSummary"]
    - path: "src/app/api/yolo/builds/route.ts"
      provides: "GET /api/yolo/builds endpoint"
      exports: ["GET", "dynamic"]
  key_links:
    - from: "src/app/api/yolo/builds/route.ts"
      to: "src/lib/queries/yolo.ts"
      via: "import getYoloBuilds"
      pattern: "import.*getYoloBuilds.*from.*queries/yolo"
    - from: "src/lib/queries/yolo.ts"
      to: "src/lib/db.ts"
      via: "getDb('yolo')"
      pattern: "getDb\\(\"yolo\"\\)"
---

<objective>
Register yolo.db in the Mission Control database layer, create a typed query module, and expose a GET API route that returns YOLO build history with status counts.

Purpose: Establish the data pipeline so the frontend page (Plan 02) can fetch build data via SWR.
Output: Working API endpoint at /api/yolo/builds returning typed JSON from yolo.db.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-yolo-dashboard/40-RESEARCH.md
@.planning/phases/40-yolo-dashboard/40-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register yolo.db and create query module</name>
  <files>src/lib/db-paths.ts, src/lib/queries/yolo.ts</files>
  <action>
**On EC2** at `~/clawd/mission-control/`:

1. **Modify `src/lib/db-paths.ts`**: Add `"yolo"` to the `DB_NAMES` tuple, add `yolo: "/home/ubuntu/clawd/yolo-dev/yolo.db"` to `DB_PATHS`, and add `yolo: "YOLO Builds"` to `DB_LABELS`. Follow the exact pattern of the existing 5 entries.

2. **Create `src/lib/queries/yolo.ts`**: Follow the `agents.ts` query pattern.

Export interfaces:
- `YoloBuild`: id (number), date (string), slug (string), name (string), description (string|null), status ("idea"|"building"|"testing"|"success"|"partial"|"failed"), techStack (string[]), linesOfCode (number|null), filesCreated (number|null), selfScore (number|null), startedAt (string|null), completedAt (string|null), durationSeconds (number|null)
- `YoloBuildSummary`: builds (YoloBuild[]), counts ({ total, success, partial, failed } all numbers)

Export function `getYoloBuilds(): YoloBuildSummary`:
- Call `getDb("yolo")`. If null, return empty summary (empty builds, zero counts).
- Query: `SELECT id, date, slug, name, description, status, tech_stack, lines_of_code, files_created, self_score, started_at, completed_at, duration_seconds FROM builds ORDER BY date DESC, id DESC`
- Map rows: camelCase field names, parse `tech_stack` from comma-separated string to `string[]` via `.split(",").map(s => s.trim())`. Handle null/empty tech_stack → empty array.
- Compute counts from the builds array: total, success (status==="success"), partial (status==="partial"), failed (status==="failed").

**IMPORTANT**: Do NOT select `build_log`, `error_log`, or `self_evaluation` — these are verbose fields excluded per CONTEXT.md decision.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/clawd/mission-control && npx tsc --noEmit src/lib/queries/yolo.ts 2>&1 | head -20"</automated>
    <manual>Verify db-paths.ts has 6 entries in DB_NAMES and yolo.ts exports YoloBuild, YoloBuildSummary, getYoloBuilds</manual>
  </verify>
  <done>db-paths.ts has yolo registered (6th database), yolo.ts compiles with no type errors, exports getYoloBuilds returning YoloBuildSummary</done>
</task>

<task type="auto">
  <name>Task 2: Create API route for YOLO builds</name>
  <files>src/app/api/yolo/builds/route.ts</files>
  <action>
**On EC2** at `~/clawd/mission-control/`:

Create `src/app/api/yolo/builds/route.ts` following the existing API route pattern (see `api/agents/route.ts`):

```typescript
import { NextResponse } from "next/server";
import { getYoloBuilds } from "@/lib/queries/yolo";

export const dynamic = "force-dynamic";

export function GET() {
  try {
    return NextResponse.json(getYoloBuilds());
  } catch (error) {
    return NextResponse.json(
      { builds: [], counts: { total: 0, success: 0, partial: 0, failed: 0 }, error: String(error) },
      { status: 500 }
    );
  }
}
```

Key points:
- `export const dynamic = "force-dynamic"` prevents Next.js static rendering (Pitfall 1 from research)
- Error response returns the same shape as success (builds array + counts) so the frontend handles it gracefully
- No query parameters needed — client-side filtering handles status filtering (research anti-pattern: avoid server-side filtering for <100 builds)

After creating the file, build and restart:
```bash
cd ~/clawd/mission-control && npm run build && systemctl --user restart mission-control
```

Then verify the endpoint works:
```bash
curl -s http://localhost:3001/api/yolo/builds | python3 -m json.tool | head -30
```

Expected: JSON with `builds` array containing at least the 001-chronicle build (id=4, status=success, score=4) and `counts` object.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "curl -sf http://localhost:3001/api/yolo/builds | python3 -c 'import sys,json; d=json.load(sys.stdin); assert \"builds\" in d and \"counts\" in d and d[\"counts\"][\"total\"] >= 1, f\"Bad response: {d}\"; print(f\"OK: {d[\"counts\"][\"total\"]} builds, chronicle={d[\"builds\"][0][\"name\"]}\")'"</automated>
    <manual>curl the endpoint and verify JSON shape, build data present, newest-first ordering</manual>
  </verify>
  <done>GET /api/yolo/builds returns JSON with builds array (including chronicle build) and counts object, endpoint is live on port 3001</done>
</task>

</tasks>

<verification>
1. `getDb("yolo")` resolves to yolo.db without error
2. API returns chronicle build with correct fields (name, status, score, techStack parsed as array)
3. Counts include total >= 1, success >= 1
4. No TypeScript compilation errors
</verification>

<success_criteria>
- yolo.db registered as 6th database in db-paths.ts
- GET /api/yolo/builds returns typed JSON with builds and counts
- Chronicle build (id=4) appears in response with all expected fields
- Mission Control rebuilt and serving the new endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/40-yolo-dashboard/40-01-SUMMARY.md`
</output>
