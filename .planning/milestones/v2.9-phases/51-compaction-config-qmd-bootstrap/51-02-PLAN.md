---
phase: 51-compaction-config-qmd-bootstrap
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified: []
autonomous: false
requirements: [COMP-03]

must_haves:
  truths:
    - "Gateway restarts cleanly and Bob responds to Slack DM within 2 minutes"
    - "No compaction loop errors appear in journalctl for 30+ minutes post-restart"
    - "Compaction fires when session crosses 8K token threshold"
  artifacts:
    - path: "journalctl output"
      provides: "Clean gateway logs post-restart"
      contains: "no compaction loop errors"
  key_links:
    - from: "openclaw-gateway.service restart"
      to: "Bob agent responsiveness"
      via: "systemctl --user restart"
      pattern: "Active: active"
    - from: "compaction config (softThresholdTokens=8000)"
      to: "compaction flush event in journalctl"
      via: "session token count crossing threshold"
      pattern: "compaction|flush|memory"
---

<objective>
Restart the gateway service to pick up config changes, verify Bob responds, run a 30-minute soak test in background, verify QMD search works post-restart, and trigger a compaction test.

Purpose: Prove the config changes from Plan 01 actually work in the live system.
Output: Verified working gateway with correct compaction behavior and QMD search.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-compaction-config-qmd-bootstrap/51-CONTEXT.md
@.planning/phases/51-compaction-config-qmd-bootstrap/51-01-SUMMARY.md
</context>

<rollback>
Compaction loop regression: `ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cp ~/.openclaw/openclaw.json.bak ~/.openclaw/openclaw.json && systemctl --user restart openclaw-gateway.service"`
QMD OOM post-restart: `ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "systemctl --user stop mission-control.service"` to free memory, then restart gateway.
</rollback>

<tasks>

<task type="auto">
  <name>Task 1: Restart gateway and start background soak test</name>
  <files>None (SSH commands only)</files>
  <action>
All commands run via SSH to ubuntu@100.72.143.9 using key ~/.ssh/clawdbot-key.pem.

TIMING: Check current time on EC2. Restart should happen at the :08-:12 mark of any hour to avoid heartbeat windows (:00/:02/:04/:06) and briefing windows (5:50-6:10 AM PT). If current time is in a conflict window, wait for the next safe gap.

1. Verify current gateway status before restart:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "systemctl --user is-active openclaw-gateway.service; date -u"
   ```

2. Restart the gateway service:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "systemctl --user restart openclaw-gateway.service"
   ```

3. Wait 10 seconds, then verify gateway is active:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "sleep 10 && systemctl --user is-active openclaw-gateway.service && echo 'Gateway running'"
   ```

4. Check initial journalctl for startup errors (last 30 lines):
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "journalctl --user -u openclaw-gateway.service --since '2 minutes ago' --no-pager | tail -30"
   ```
   Look for: no "compaction loop" errors, no crash restarts, clean startup sequence.

5. Start 30-minute soak test in background — monitors for compaction loop errors:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "nohup bash -c '
     LOGFILE=/tmp/soak-test-51.log
     echo \"Soak test started: \$(date -u)\" > \$LOGFILE
     for i in \$(seq 1 30); do
       sleep 60
       ERRORS=\$(journalctl --user -u openclaw-gateway.service --since \"\$((i))min ago\" --no-pager 2>/dev/null | grep -ci \"compaction.*loop\\|compaction.*error\\|repeated.*compaction\" || true)
       echo \"Minute \$i: \$ERRORS compaction errors\" >> \$LOGFILE
       if [ \"\$ERRORS\" -gt 0 ]; then
         echo \"WARNING: Compaction loop detected at minute \$i\" >> \$LOGFILE
       fi
     done
     echo \"Soak test completed: \$(date -u)\" >> \$LOGFILE
     echo \"RESULT: \$(grep -c WARNING \$LOGFILE) warnings in 30 minutes\" >> \$LOGFILE
   ' > /dev/null 2>&1 &"
   ```
   This runs in background on EC2. Check results later with `cat /tmp/soak-test-51.log`.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "systemctl --user is-active openclaw-gateway.service" | grep -q "active" && echo "PASS: Gateway running" || echo "FAIL: Gateway not active"</automated>
  </verify>
  <done>Gateway restarted cleanly, is active, no startup errors in journalctl. 30-minute soak test running in background at /tmp/soak-test-51.log.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User DMs Bob to re-establish session</name>
  <what-built>Gateway restarted with new compaction + search config. Bob needs a DM to re-establish session.</what-built>
  <how-to-verify>
    1. Open Slack and send Bob a DM (any message, e.g., "ping")
    2. Bob should respond within 2 minutes
    3. If Bob responds, type "approved" to continue
    4. If Bob does not respond within 2 minutes, describe what happened
  </how-to-verify>
  <resume-signal>Type "approved" if Bob responded, or describe the issue</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify QMD search post-restart and trigger compaction test</name>
  <files>None (SSH commands only)</files>
  <action>
All commands run via SSH to ubuntu@100.72.143.9.

PART A — QMD search verification (runs parallel to soak test):

1. Test QMD search with the decided query mix (system knowledge + recent events):
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'Andy' 2>&1 | head -20"
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'content pipeline' 2>&1 | head -20"
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'Govee' 2>&1 | head -20"
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'v2.8' 2>&1 | head -20"
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'mission control' 2>&1 | head -20"
   ```
   At least 3 of 5 queries should return non-empty results. Record which queries returned results and which were empty.

PART B — Compaction trigger test:

2. The compaction test proves COMP-03: that the new softThresholdTokens=8K actually fires during a session. The approach:
   - Send Bob a series of messages via Slack DM that will generate enough conversation to cross 8K tokens
   - Monitor journalctl for a compaction/flush event

3. Tell the user to send Bob a long conversation in Slack DM — or use the gateway API if available. The conversation needs to be substantive enough to cross 8K tokens (~6000 words of back-and-forth). Suggest asking Bob to:
   - "Tell me everything you know about my content pipeline setup"
   - "Now tell me about the email integration"
   - "Now summarize the entire YOLO dev system"
   Each response from Bob + the prompts should accumulate tokens.

4. While the conversation is happening, tail journalctl for compaction events:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "journalctl --user -u openclaw-gateway.service -f --no-pager 2>&1 | grep -i 'compact\|flush\|memory\|threshold'" &
   ```
   Run this in the background and check for flush/compaction events. Look for log lines containing "compaction", "flush", "memory", or "threshold".

5. After the conversation, check journalctl for compaction evidence:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "journalctl --user -u openclaw-gateway.service --since '15 minutes ago' --no-pager | grep -i 'compact\|flush\|memory\|threshold'"
   ```

PART C — Check soak test results:

6. After 30+ minutes have elapsed since restart, check the soak test log:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cat /tmp/soak-test-51.log 2>/dev/null || echo 'Soak test still running'"
   ```
   Expected: "RESULT: 0 warnings in 30 minutes"

7. Final journalctl check — look for any compaction loop patterns in full post-restart window:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "journalctl --user -u openclaw-gateway.service --since '35 minutes ago' --no-pager | grep -ci 'compaction.*loop\|compaction.*error\|repeated.*compaction'"
   ```
   Expected: 0

NOTE: The compaction trigger test (Part B) requires the user to have an extended conversation with Bob. If the user prefers not to do this now, document that COMP-03 was partially verified (config set, no loop errors) and the full trigger test can happen organically during normal use.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'Andy' 2>&1 | head -5" | grep -q -v "^$" && echo "PASS: QMD search returns results" || echo "FAIL: QMD search empty"</automated>
  </verify>
  <done>QMD search returns results for 3+ of 5 test queries. Soak test shows 0 compaction loop warnings over 30 minutes. Compaction trigger test shows flush event in journalctl (or documented as pending organic verification). All 6 verification checks from CONTEXT.md passed.</done>
</task>

</tasks>

<verification>
1. Gateway service is active: `systemctl --user is-active openclaw-gateway.service` = "active"
2. Bob responds to Slack DM within 2 minutes of restart
3. QMD search returns results for "Andy", "content pipeline", "mission control" (at least 3/5 queries)
4. Soak test log shows 0 compaction loop warnings over 30 minutes
5. journalctl shows clean operation — no compaction loop errors
6. Compaction flush event appears in journalctl during/after extended conversation
</verification>

<success_criteria>
- Gateway restarted cleanly at safe time window
- Bob responds to Slack DM (session re-established)
- QMD search works post-restart with meaningful results
- 30-minute soak test passes with 0 compaction loop errors
- Compaction threshold fires correctly (or documented as pending)
- All Phase 51 requirements (COMP-01 through SRCH-02) verified
</success_criteria>

<output>
After completion, create `.planning/phases/51-compaction-config-qmd-bootstrap/51-02-SUMMARY.md`
</output>
