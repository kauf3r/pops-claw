---
phase: 33-content-pipeline-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/clawd/agents/rangeos/TOPIC_RESEARCH.md
  - ~/clawd/agents/quill/WRITING_SESSION.md
  - ~/clawd/agents/sage/REVIEW_SESSION.md
  - ~/clawd/agents/ezra/PUBLISH_SESSION.md
  - ~/clawd/agents/ops/STUCK_CHECK.md
  - ~/clawd/agents/ops/PIPELINE_REPORT.md
  - ~/clawd/agents/quill/content.db (delete 0-byte)
  - ~/clawd/agents/ezra/content.db (delete 0-byte)
autonomous: true
requirements: [CP-01, CP-02]

must_haves:
  truths:
    - "content.db bind-mount is verified and 0-byte stubs cleaned up"
    - "All 5 content cron jobs post summaries to Slack successfully using channel IDs"
    - "Each session instruction file references channel:ID format, not #channel-name"
    - "Sentinel ops session files use channel:C0AD485E50Q for #ops delivery"
  artifacts:
    - path: "~/clawd/agents/rangeos/TOPIC_RESEARCH.md"
      provides: "Vector session instructions with channel:C0AC3HB82P5"
      contains: "channel:C0AC3HB82P5"
    - path: "~/clawd/agents/quill/WRITING_SESSION.md"
      provides: "Quill session instructions with channel:C0ADWCMU5F0"
      contains: "channel:C0ADWCMU5F0"
    - path: "~/clawd/agents/sage/REVIEW_SESSION.md"
      provides: "Sage session instructions with channel:C0ADWCMU5F0"
      contains: "channel:C0ADWCMU5F0"
    - path: "~/clawd/agents/ezra/PUBLISH_SESSION.md"
      provides: "Ezra session instructions with channel:C0ADWCMU5F0"
      contains: "channel:C0ADWCMU5F0"
    - path: "~/clawd/agents/ops/"
      provides: "Sentinel session instructions with channel:C0AD485E50Q"
      contains: "channel:C0AD485E50Q"
  key_links:
    - from: "cron job (topic-research)"
      to: "Slack #range-ops"
      via: "session instruction channel reference"
      pattern: "channel:C0AC3HB82P5"
    - from: "cron jobs (writing/review/publish)"
      to: "Slack #content-pipeline"
      via: "session instruction channel reference"
      pattern: "channel:C0ADWCMU5F0"
    - from: "cron jobs (stuck-check/pipeline-report)"
      to: "Slack #ops"
      via: "session instruction channel reference"
      pattern: "channel:C0AD485E50Q"
---

<objective>
Verify content.db bind-mount, clean up stale 0-byte DB stubs, and fix Slack channel references in ALL content and ops agent session instruction files so all 5 cron summaries deliver successfully.

Purpose: Content crons run but fail to post summaries to Slack because session files reference channel names (#content-pipeline, #ops) instead of channel IDs (channel:C0ADWCMU5F0, channel:C0AD485E50Q). Fixing this makes the pipeline observable without checking logs.
Output: 6 updated session instruction files (4 content agents + 2 Sentinel ops) with correct channel IDs, 0-byte stubs removed, bind-mount verified.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-content-pipeline-improvements/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify bind-mount and clean up 0-byte stubs</name>
  <files>
    ~/clawd/agents/quill/content.db
    ~/clawd/agents/ezra/content.db
    ~/clawd/agents/main/content.db
  </files>
  <action>
SSH to EC2 (100.72.143.9) and verify the content.db bind-mount is correct:

1. Confirm `~/clawd/content.db` exists and is a real SQLite database (not 0-byte):
   `file ~/clawd/content.db` should show "SQLite 3.x database"
   `sqlite3 ~/clawd/content.db "SELECT count(*) FROM topics;"` should return > 0

2. Confirm the bind-mount in `~/.openclaw/openclaw.json` has the entry:
   `~/clawd/content.db:/workspace/content.db:rw` in `agents.defaults.sandbox.docker.binds`

3. Delete 0-byte content.db stubs that cause confusion during debugging:
   `rm -f ~/clawd/agents/quill/content.db ~/clawd/agents/ezra/content.db`
   (These are masked by Docker bind-mount at runtime but confuse manual inspection)

4. Check if `~/clawd/agents/main/content.db` exists -- if it's 0-byte or was renamed to .old (per MEMORY.md), confirm it's gone or .old. If a real DB exists there, note it but do NOT delete (could be stale data worth reviewing).

5. Verify Mission Control db-paths.ts points to `~/clawd/content.db` (already confirmed in research, just double-check via SSH):
   `grep content ~/clawd/mission-control/src/lib/db-paths.ts`
  </action>
  <verify>
`file ~/clawd/content.db` returns "SQLite 3.x database"
`ls -la ~/clawd/agents/quill/content.db 2>/dev/null` returns "No such file"
`ls -la ~/clawd/agents/ezra/content.db 2>/dev/null` returns "No such file"
`sqlite3 ~/clawd/content.db "SELECT count(*) FROM topics;"` returns a number > 0
  </verify>
  <done>content.db bind-mount verified pointing to ~/clawd/content.db, 0-byte stubs deleted, MC db-paths confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Fix Slack channel references in all content AND ops session files</name>
  <files>
    ~/clawd/agents/rangeos/TOPIC_RESEARCH.md
    ~/clawd/agents/quill/WRITING_SESSION.md
    ~/clawd/agents/sage/REVIEW_SESSION.md
    ~/clawd/agents/ezra/PUBLISH_SESSION.md
    ~/clawd/agents/ops/
  </files>
  <action>
SSH to EC2 and update ALL agent session instruction files to use channel IDs instead of channel names. This covers both content agents AND Sentinel ops agent.

**Step 1: Read all content agent session files:**
- `cat ~/clawd/agents/rangeos/TOPIC_RESEARCH.md`
- `cat ~/clawd/agents/quill/WRITING_SESSION.md`
- `cat ~/clawd/agents/sage/REVIEW_SESSION.md`
- `cat ~/clawd/agents/ezra/PUBLISH_SESSION.md`

**Step 2: Read Sentinel ops session files:**
- `ls ~/clawd/agents/ops/` to discover all session instruction files
- Read each .md file in `~/clawd/agents/ops/` (likely includes files for stuck-check and pipeline-report crons)
- These should reference `#ops` channel which needs to become `channel:C0AD485E50Q`

**Step 3: Fix content agent files.** For each file, find the section where the agent is told to post results to Slack and replace channel name references with channel ID format:

- Vector (TOPIC_RESEARCH.md): #range-ops -> channel:C0AC3HB82P5
- Quill (WRITING_SESSION.md): #content-pipeline -> channel:C0ADWCMU5F0
- Sage (REVIEW_SESSION.md): #content-pipeline -> channel:C0ADWCMU5F0
- Ezra (PUBLISH_SESSION.md): #content-pipeline -> channel:C0ADWCMU5F0

**Step 4: Fix Sentinel ops session files.** For each session file in `~/clawd/agents/ops/`:
- Replace any `#ops` channel name references with `channel:C0AD485E50Q`
- Also check for `#content-pipeline` references (pipeline-report may cross-post) and fix to `channel:C0ADWCMU5F0` if present

**Step 5: Add channel ID reference section** near the top of each file:
```
## Slack Channels
- Post results to: channel:CXXXXXXXXXX
```

**Step 6: Verify no stale channel names remain:**
After updating, run:
`grep -ri "#content-pipeline\|#range-ops\|#ops" ~/clawd/agents/rangeos/*.md ~/clawd/agents/quill/*.md ~/clawd/agents/sage/*.md ~/clawd/agents/ezra/*.md ~/clawd/agents/ops/*.md`
Should return NO matches.

**Step 7: Test with manual cron runs.** Get cron job IDs first:
`/home/ubuntu/.npm-global/bin/openclaw cron list`

Then run at least one content cron and one ops cron manually:
- `/home/ubuntu/.npm-global/bin/openclaw cron run --id <writing-check-id>` (content pipeline cron)
- `/home/ubuntu/.npm-global/bin/openclaw cron run --id <stuck-check-id>` OR `<pipeline-report-id>` (ops cron)

Check each cron run summary for Slack delivery success (no "channel id" or "target" errors).
  </action>
  <verify>
`grep -i "channel:" ~/clawd/agents/rangeos/TOPIC_RESEARCH.md` shows channel:C0AC3HB82P5
`grep -i "channel:" ~/clawd/agents/quill/WRITING_SESSION.md` shows channel:C0ADWCMU5F0
`grep -i "channel:" ~/clawd/agents/sage/REVIEW_SESSION.md` shows channel:C0ADWCMU5F0
`grep -i "channel:" ~/clawd/agents/ezra/PUBLISH_SESSION.md` shows channel:C0ADWCMU5F0
`grep -ri "channel:" ~/clawd/agents/ops/*.md` shows channel:C0AD485E50Q
`grep -ri "#content-pipeline\|#range-ops\|#ops" ~/clawd/agents/rangeos/*.md ~/clawd/agents/quill/*.md ~/clawd/agents/sage/*.md ~/clawd/agents/ezra/*.md ~/clawd/agents/ops/*.md` returns no matches (no stale channel names)
Manual content cron run produces output without "channel id" or "target" errors
Manual ops cron run (stuck-check or pipeline-report) produces output without "channel id" or "target" errors
  </verify>
  <done>All 6 session instruction files (4 content + 2 ops) use channel:ID format, no stale channel names remain, at least one content cron and one ops cron deliver to Slack successfully via manual run</done>
</task>

</tasks>

<verification>
- content.db is a valid SQLite database at ~/clawd/content.db with real data
- No 0-byte content.db stubs exist in agent workspace directories
- All 6 session instruction files (4 content agents + Sentinel ops) reference Slack channels by ID (channel:CXXXXXXXXXX)
- No session instruction file contains #content-pipeline, #range-ops, or #ops as channel references
- At least one content cron run posts successfully to Slack (#content-pipeline or #range-ops)
- At least one ops cron run posts successfully to Slack (#ops)
</verification>

<success_criteria>
Content infrastructure is verified and ALL cron Slack notifications work -- both content agents posting to #content-pipeline/#range-ops AND Sentinel posting to #ops. The pipeline runs silently no more.
</success_criteria>

<output>
After completion, create `.planning/phases/33-content-pipeline-improvements/33-01-SUMMARY.md`
</output>
