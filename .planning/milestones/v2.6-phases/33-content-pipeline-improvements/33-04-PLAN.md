---
phase: 33-content-pipeline-improvements
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: ["~/.openclaw/cron/jobs.json"]
autonomous: false
requirements: [CP-02]
gap_closure: true

must_haves:
  truths:
    - "All 5 content cron payload messages in jobs.json use channel:CXXXXXXXXXX format"
    - "No #channel-name references remain in any cron payload message"
    - "topic-research payload references channel:C0AC3HB82P5 (range-ops), not content-pipeline"
    - "Review-check cron delivers summary to Slack without gateway error"
  artifacts:
    - path: "~/.openclaw/cron/jobs.json"
      provides: "Cron job definitions with correct channel:ID payload messages"
      contains: "channel:C0ADWCMU5F0"
  key_links:
    - from: "~/.openclaw/cron/jobs.json cron payload"
      to: "Slack channel delivery"
      via: "channel:ID in message field parsed by gateway"
      pattern: "channel:C0[A-Z0-9]+"
---

<objective>
Fix cron payload messages in jobs.json to use channel:ID format, closing the last gap in Slack delivery.

Purpose: Plan 33-01 fixed session instruction files but missed the cron payload messages in ~/.openclaw/cron/jobs.json. The gateway rejects #channel-name format at the tool call level, causing "Slack channels require a channel id" errors before the agent even reads the corrected session file. This is the root cause of all remaining Slack delivery failures.

Output: Updated jobs.json with all 5 content cron payloads using channel:ID format, verified by a manual cron run.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-content-pipeline-improvements/33-VERIFICATION.md
@.planning/phases/33-content-pipeline-improvements/33-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cron payload messages in jobs.json to use channel:ID format</name>
  <files>~/.openclaw/cron/jobs.json (on EC2 at 100.72.143.9)</files>
  <action>
SSH to EC2 (100.72.143.9) using key ~/.ssh/clawdbot-key.pem as ubuntu.

1. Read the current jobs.json:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cat ~/.openclaw/cron/jobs.json"
   ```

2. Back up before editing:
   ```
   ssh ... "cp ~/.openclaw/cron/jobs.json ~/.openclaw/cron/jobs.json.bak-$(date +%Y%m%d)"
   ```

3. Update the `message` field for these 5 cron jobs (use jq, sed, or python — whatever gets clean edits):

   - **topic-research**: Replace any occurrence of `#content-pipeline` in the message with `channel:C0AC3HB82P5` (Vector posts to #range-ops, NOT #content-pipeline — this was also a wrong-channel bug)
   - **writing-check**: Replace `#content-pipeline` with `channel:C0ADWCMU5F0`
   - **review-check**: Replace `#content-pipeline` with `channel:C0ADWCMU5F0`
   - **pipeline-report**: Replace `#ops` with `channel:C0AD485E50Q`
   - **stuck-check**: Replace `#content-pipeline (C0ADWCMU5F0)` with `channel:C0ADWCMU5F0` (remove the hybrid format entirely)

   Channel ID reference:
   - #range-ops = C0AC3HB82P5 (Vector/rangeos)
   - #content-pipeline = C0ADWCMU5F0 (Quill, Sage, Ezra)
   - #ops = C0AD485E50Q (Sentinel/ops)

   IMPORTANT: Only modify the `message` field text. Do NOT change schedule, sessionTarget, agentId, kind, or any other field. The message field is a string that the cron system includes when starting the agent session — it is the first instruction the agent sees.

   IMPORTANT: Preserve valid JSON. After editing, validate with `python3 -c "import json; json.load(open('/path/to/jobs.json'))"` or `jq . jobs.json > /dev/null`.

4. Verify no stale channel names remain in any payload:
   ```
   ssh ... "grep -c '#content-pipeline\|#range-ops\|#ops' ~/.openclaw/cron/jobs.json"
   ```
   Expected: 0 matches (exit code 1 from grep = no matches = correct)

5. Verify channel:ID format appears in the updated payloads:
   ```
   ssh ... "grep -o 'channel:C0[A-Z0-9]*' ~/.openclaw/cron/jobs.json | sort"
   ```
   Expected: channel:C0AC3HB82P5, channel:C0AD485E50Q, channel:C0ADWCMU5F0 (multiple occurrences)
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "python3 -c \"import json; d=json.load(open('/home/ubuntu/.openclaw/cron/jobs.json')); print('VALID JSON')\" && grep -c '#content-pipeline\|#range-ops\|#ops' /home/ubuntu/.openclaw/cron/jobs.json || echo 'NO_STALE_CHANNELS (good)'"</automated>
  </verify>
  <done>All 5 content cron payload messages use channel:CXXXXXXXXXX format. Zero #channel-name references remain in jobs.json. topic-research correctly references channel:C0AC3HB82P5 (range-ops). JSON is valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end Slack delivery with a manual cron run</name>
  <files>~/.openclaw/cron/jobs.json (on EC2 — read-only verification)</files>
  <action>
Human verifies that the jobs.json payload fix results in successful Slack delivery. The executor should trigger a manual cron run before presenting this checkpoint:
```
ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "/home/ubuntu/.npm-global/bin/openclaw cron run review-check"
```
Then check gateway log for errors:
```
ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "journalctl --user -u openclaw-gateway.service --since '3 min ago' --no-pager | grep -i 'message failed\|channel id'"
```
Present results to user for Slack channel verification.
  </action>
  <verify>User confirms Slack message arrived in #content-pipeline without gateway errors.</verify>
  <done>Review-check cron delivers summary to #content-pipeline (C0ADWCMU5F0) without "Slack channels require a channel id" errors. CP-02 fully satisfied.</done>
  <what-built>Updated all 5 content cron payload messages in jobs.json from #channel-name format to channel:ID format. This was the root cause of all Slack delivery failures — the gateway rejected #channel-name at the tool call level before agents could read the corrected session instruction files.</what-built>
  <how-to-verify>
1. SSH to EC2 and run the review-check cron manually:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "/home/ubuntu/.npm-global/bin/openclaw cron run review-check"
   ```
2. Watch the #content-pipeline Slack channel for a review summary message from Sage within 2-3 minutes.
3. Check gateway log for errors:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "journalctl --user -u openclaw-gateway.service --since '2 min ago' --no-pager | grep -i 'message failed\|channel id'"
   ```
   Expected: No "message failed" or "channel id" errors.
4. (Optional) Run topic-research cron and confirm the summary appears in #range-ops (C0AC3HB82P5), NOT in #content-pipeline.
  </how-to-verify>
  <resume-signal>Type "approved" if Slack messages arrive in the correct channels without gateway errors, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- jobs.json is valid JSON after edits
- Zero occurrences of #content-pipeline, #range-ops, or #ops as bare channel names in jobs.json
- channel:C0AC3HB82P5 present in topic-research payload
- channel:C0ADWCMU5F0 present in writing-check, review-check, stuck-check payloads
- channel:C0AD485E50Q present in pipeline-report payload
- Manual cron run delivers to Slack without gateway errors
</verification>

<success_criteria>
All 5 content cron jobs have payload messages using channel:ID format. A manual cron run (review-check) successfully delivers a summary to the correct Slack channel without "Slack channels require a channel id" errors in the gateway log. CP-02 requirement is fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/33-content-pipeline-improvements/33-04-SUMMARY.md`
</output>
