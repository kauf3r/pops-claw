---
phase: 47-build-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/47-build-artifacts/47-01-SUMMARY.md
autonomous: false
requirements:
  - PREV-01

must_haves:
  truths:
    - "Builds with index.html show a sandboxed iframe preview on the detail page"
    - "Builds without index.html do NOT show an iframe"
    - "Open in new tab link works for HTML builds"
  artifacts:
    - path: "src/app/yolo/[slug]/page.tsx"
      provides: "Iframe preview rendering"
      contains: "sandbox=\"allow-scripts\""
    - path: "src/app/api/yolo/files/[...path]/route.ts"
      provides: "File serving with correct MIME types"
      contains: "text/html"
  key_links:
    - from: "src/app/yolo/[slug]/page.tsx"
      to: "/api/yolo/files/{slug}/index.html"
      via: "iframe src attribute"
      pattern: "api/yolo/files.*index\\.html"
---

<objective>
Verify that PREV-01 (iframe preview of HTML builds) is fully working as implemented in Phase 44.

Purpose: Research confirmed the iframe preview code already exists from Phase 44. This plan verifies it works correctly with real builds rather than re-implementing anything.
Output: Verification evidence (screenshots/curl output) confirming PREV-01 is satisfied.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-build-artifacts/47-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify iframe preview implementation</name>
  <files>.planning/phases/47-build-artifacts/47-01-SUMMARY.md</files>
  <action>
SSH to EC2 (100.72.143.9) and verify PREV-01 is working:

1. Query yolo.db to find builds with and without index.html:
   ```
   sqlite3 ~/clawd/agents/main/yolo-dev/yolo.db "SELECT slug, name FROM builds"
   ```
   Then check which build directories have index.html:
   ```
   for d in ~/clawd/agents/main/yolo-dev/*/; do [ -f "$d/index.html" ] && echo "HTML: $(basename $d)"; done
   ```

2. Verify the API serves HTML files correctly:
   ```
   curl -s -o /dev/null -w "%{http_code} %{content_type}" http://127.0.0.1:3001/api/yolo/files/007-expense-tracker-dashboard/index.html
   ```
   Expected: 200 with text/html content type.

3. Verify the build detail API returns hasHtml=true for HTML builds:
   ```
   curl -s http://127.0.0.1:3001/api/yolo/builds/007-expense-tracker-dashboard | python3 -c "import sys,json; d=json.load(sys.stdin); print('hasHtml:', d.get('hasHtml'))"
   ```

4. Verify the iframe code exists in the page component:
   ```
   grep -n "sandbox.*allow-scripts" ~/clawd/mission-control/src/app/yolo/\[slug\]/page.tsx
   grep -n "Open in new tab" ~/clawd/mission-control/src/app/yolo/\[slug\]/page.tsx
   ```

5. Test a build WITHOUT index.html to confirm no iframe appears:
   ```
   curl -s http://127.0.0.1:3001/api/yolo/builds/001-chronicle | python3 -c "import sys,json; d=json.load(sys.stdin); print('hasHtml:', d.get('hasHtml'))"
   ```
   Expected: hasHtml=false (or absent).

Record all output as verification evidence.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3001/api/yolo/files/007-expense-tracker-dashboard/index.html"</automated>
  </verify>
  <done>API returns 200 for HTML file serving, hasHtml=true for HTML builds, hasHtml=false for non-HTML builds, iframe sandbox code confirmed in page.tsx</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Verified that PREV-01 (iframe preview) is already working from Phase 44. API serves HTML files with correct MIME types, detail page renders sandboxed iframe for HTML builds, and non-HTML builds show no iframe.</what-built>
  <how-to-verify>
    1. Open Mission Control via SSH tunnel: ssh -L 3001:127.0.0.1:3001 -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9
    2. Visit http://localhost:3001/yolo/007-expense-tracker-dashboard
    3. Confirm: iframe preview visible with "Live Preview" header and "Open in new tab" link
    4. Click "Open in new tab" — HTML should load in a new browser tab
    5. Visit http://localhost:3001/yolo/001-chronicle (non-HTML build)
    6. Confirm: NO iframe preview section visible
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- curl to /api/yolo/files/{slug}/index.html returns 200 with text/html
- hasHtml=true for builds with index.html, false for builds without
- iframe sandbox="allow-scripts" present in page.tsx
- "Open in new tab" link present in page.tsx
</verification>

<success_criteria>
PREV-01 confirmed working: HTML builds show iframe preview, non-HTML builds do not, "Open in new tab" link functional.
</success_criteria>

<output>
After completion, create `.planning/phases/47-build-artifacts/47-01-SUMMARY.md`
</output>
