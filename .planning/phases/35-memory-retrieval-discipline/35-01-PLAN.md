---
phase: 35-memory-retrieval-discipline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/clawd/AGENTS.md
  - ~/clawd/LEARNINGS.md
autonomous: true
requirements:
  - MEM-03
  - MEM-04

must_haves:
  truths:
    - "Global AGENTS.md contains a Memory Protocol section with 3-4 bullet retrieval instructions"
    - "LEARNINGS.md exists with 15-25 seeded entries across 4-5 categories drawn from real operational knowledge"
    - "LEARNINGS.md contains a MARKER test entry that looks like a real operational lesson (not obviously synthetic)"
    - "Memory Protocol instructs a prioritized cascade: LEARNINGS.md first, daily logs (7 days), docs/ directory, with early stop"
    - "LEARNINGS.md is placed where all agents can access it (global workspace or verified bind-mount)"
  artifacts:
    - path: "~/clawd/AGENTS.md"
      provides: "Memory Protocol section in boot sequence"
      contains: "Memory Protocol"
    - path: "~/clawd/LEARNINGS.md"
      provides: "Seeded operational knowledge base"
      contains: "Operational Learnings"
  key_links:
    - from: "~/clawd/AGENTS.md Memory Protocol"
      to: "~/clawd/LEARNINGS.md"
      via: "Retrieval instruction referencing LEARNINGS.md as first cascade tier"
      pattern: "LEARNINGS.md"
    - from: "~/clawd/AGENTS.md Memory Protocol"
      to: "~/clawd/agents/main/memory/"
      via: "Retrieval instruction referencing daily logs as second cascade tier"
      pattern: "daily logs"
---

<objective>
Add memory retrieval discipline to the agent boot sequence and seed a shared operational knowledge base.

Purpose: Agents currently write memory (daily logs, MEMORY.md) but have no explicit instructions to read it back before starting tasks. This plan closes the read gap by adding a Memory Protocol to the global AGENTS.md and creating a seeded LEARNINGS.md that all agents can reference.

Output: Updated AGENTS.md with Memory Protocol section, new LEARNINGS.md with 15-25 real operational entries and a MARKER test entry.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-memory-retrieval-discipline/35-RESEARCH.md
@.planning/phases/35-memory-retrieval-discipline/35-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve sandbox visibility and add Memory Protocol to AGENTS.md</name>
  <files>~/clawd/AGENTS.md (on EC2 via SSH)</files>
  <action>
First, resolve the highest-priority open question from research: verify whether isolated cron sessions (Docker sandbox) can see the global ~/clawd/AGENTS.md. SSH to EC2 and check:

```bash
ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cat ~/.openclaw/openclaw.json | python3 -c \"import sys,json; c=json.load(sys.stdin); print(json.dumps(c.get('agents',{}).get('defaults',{}).get('sandbox',{}), indent=2))\""
```

Check if ~/clawd/ is bind-mounted into the sandbox (not just ~/clawd/agents/{agent}/). If the global AGENTS.md is NOT visible to sandboxed agents, note this finding -- the Memory Protocol section may need to be added to per-agent AGENTS.md files as a fallback (but try global first since research says OpenClaw loads it for all agents).

Also determine the correct LEARNINGS.md location: if ~/clawd/ is visible globally, place at ~/clawd/LEARNINGS.md. If only per-agent workspaces are visible, place at ~/clawd/agents/main/LEARNINGS.md and create symlinks or bind-mounts for content agents.

Then read the current global AGENTS.md:

```bash
ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cat ~/clawd/AGENTS.md"
```

Add a "Memory Protocol" section to the global AGENTS.md. Per user decisions:
- Place it in the "Every Session" area, after the existing boot sequence (read SOUL.md, USER.md, memory, MEMORY.md)
- Keep it to 3-4 bullet points (not a full procedure doc)
- Search order: LEARNINGS.md first (quick scan), daily logs (7 days, only if task references past work), docs/ directory (if not found elsewhere)
- Stop when relevant context found -- cascade, don't exhaustively read everything
- If nothing found: proceed with best judgment, note gap in daily log
- Do NOT narrate failed searches (silent when empty per user decision)

Use the wording from research Example 1 as a starting point, adjusted for the resolved LEARNINGS.md path. The section should add no more than 10-15 lines to keep within the token budget freed by Phase 34.

Write via SSH using tee (not heredoc with cat, per lessons learned):

```bash
# Read current file, insert Memory Protocol section at the right location, write back
```

IMPORTANT: Do NOT increase the boot auto-load budget. LEARNINGS.md should be checked on-demand during the cascade, NOT auto-loaded at session start. The Memory Protocol bullets instruct the agent to search when relevant, not to read everything every session.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "grep -c 'Memory Protocol' ~/clawd/AGENTS.md && wc -l ~/clawd/AGENTS.md"</automated>
    <manual>Verify Memory Protocol section has 3-4 bullets, mentions LEARNINGS.md -> daily logs -> docs/ cascade order, and includes early-stop instruction</manual>
  </verify>
  <done>Global AGENTS.md contains a Memory Protocol section with 3-4 retrieval bullets implementing the prioritized cascade with early stop. File is under 210 lines (was 191, adding ~10-15 lines).</done>
</task>

<task type="auto">
  <name>Task 2: Create and seed LEARNINGS.md with MARKER entry</name>
  <files>~/clawd/LEARNINGS.md (or ~/clawd/agents/main/LEARNINGS.md -- path determined by Task 1) (on EC2 via SSH)</files>
  <action>
Create LEARNINGS.md at the location determined by Task 1's sandbox visibility check.

Seed with 15-25 entries across 4-5 categories. Per user decisions:
- Format: categorized one-liners with topic sections
- Each entry: bullet with context + lesson, ~2-3 lines max
- Source: existing project MEMORY.md "Lessons Learned" + docs/ files from Phase 34
- Size budget: soft cap 100 lines

Use the category structure from research (adjusted based on actual knowledge):

**Categories and seed sources:**

1. **API & Auth Gotchas** (from MEMORY.md Lessons Learned):
   - gog token corruption during migration -- remove and re-auth with --manual
   - gog auth add --manual broken (v0.9.0) -- construct OAuth URL manually, curl token exchange, gog auth tokens import
   - GCP OAuth test users -- must add in Console AND possibly whitelist in Workspace Admin
   - Gateway restart clears DM sessions -- user must DM Bob first to re-establish

2. **Cron Patterns** (from MEMORY.md + Phase 34 docs):
   - Session type matters: main (host access) vs isolated (Docker sandbox)
   - gateway.remote.url required for CLI commands (tailnet bind, not localhost)
   - Reference-doc cron pattern: complex instructions in workspace .md files, cron message just says "follow instructions in X.md"
   - DM sessions break after gateway restart -- cron sessions_send fails silently

3. **Infrastructure** (from MEMORY.md Lessons Learned):
   - sshd is socket-activated -- do NOT create socket override conf
   - cloud-boothook runs before services start -- systemctl restart in boothook fails silently
   - OOM recovery path: CloudShell -> SSM role -> open SG -> SSH via public IP
   - 2GB swap at /swapfile prevents OOM recurrence
   - Tailscale offline: try restart first, EC2 stop/start forces fresh network stack

4. **Content Pipeline** (from Phase 12-18 history):
   - CP-05 locking: BEGIN IMMEDIATE for topic/article claims, double-check WHERE clause
   - Review requires article body text -- empty body/content articles cannot be scored
   - Content agents share #content-pipeline channel with first-match routing

5. **User Preferences** (from MEMORY.md + project context):
   - Andy prefers concise verbal explanations over diagrams
   - Lead with possibilities, not constraints
   - Externalize all decisions to files -- never assume he remembers prior sessions

Include a **MARKER test entry** in the Infrastructure category. Per research Example 4 and anti-pattern guidance, make it a realistic-looking operational lesson (NOT obviously synthetic like "MARKER-TEST-12345"):

```
- **Tailscale DNS on EC2**: If `tailscale status` shows "offline" but the instance is running, try `sudo systemctl restart tailscaled` first. If that fails, EC2 stop/start forces a fresh network stack (reboot alone is insufficient).
```

This is real knowledge rephrased from existing MEMORY.md. Testing: ask Bob "What should I try if Tailscale shows offline on the EC2?" and verify he references this entry.

Write the file via SSH. Target 80-100 lines total (under the 100-line soft cap).

After creating LEARNINGS.md, verify it's accessible from the expected sandbox path by checking the bind-mount configuration determined in Task 1.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "wc -l ~/clawd/LEARNINGS.md && grep -c '^\- \*\*' ~/clawd/LEARNINGS.md && grep -c 'Tailscale DNS on EC2' ~/clawd/LEARNINGS.md"</automated>
    <manual>Verify 15-25 bullet entries across 4-5 categories, MARKER entry present in Infrastructure section, file under 100 lines</manual>
  </verify>
  <done>LEARNINGS.md exists with 15-25 seeded entries across 4-5 categories, all drawn from real operational knowledge. Contains a MARKER entry ("Tailscale DNS on EC2") that is verifiable by asking Bob. File is under 100 lines (soft cap).</done>
</task>

</tasks>

<verification>
1. `grep 'Memory Protocol' ~/clawd/AGENTS.md` returns a match
2. `wc -l ~/clawd/AGENTS.md` shows file is under 210 lines (was 191)
3. LEARNINGS.md exists at the determined global path
4. `wc -l` on LEARNINGS.md shows under 100 lines
5. `grep -c '^\- \*\*'` on LEARNINGS.md shows 15-25 entries
6. MARKER entry present: `grep 'Tailscale DNS on EC2' ~/clawd/LEARNINGS.md`
7. Memory Protocol references LEARNINGS.md, daily logs, and docs/ in that order
</verification>

<success_criteria>
- Global AGENTS.md has Memory Protocol section (3-4 bullets, cascade order, early stop)
- LEARNINGS.md seeded with 15-25 real operational entries across 4-5 categories
- MARKER test entry embedded naturally in Infrastructure category
- Total addition to boot sequence is under 15 lines (preserving Phase 34 token budget win)
- LEARNINGS.md accessible to all agents (verified via sandbox config)
</success_criteria>

<output>
After completion, create `.planning/phases/35-memory-retrieval-discipline/35-01-SUMMARY.md`
</output>
