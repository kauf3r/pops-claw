---
phase: 35-memory-retrieval-discipline
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - ~/clawd/agents/quill/BOOTSTRAP.md
  - ~/clawd/agents/sage/BOOTSTRAP.md
  - ~/clawd/agents/ezra/BOOTSTRAP.md
autonomous: true
requirements:
  - MEM-05

must_haves:
  truths:
    - "Quill's BOOTSTRAP.md contains pipeline-specific writing context (not the generic Hello World template)"
    - "Sage has a BOOTSTRAP.md with editorial review context and scoring criteria references"
    - "Ezra has a BOOTSTRAP.md with publishing workflow context and WP draft references"
    - "Each BOOTSTRAP.md includes a SQL query for checking current pipeline state at session start"
    - "Each BOOTSTRAP.md references its corresponding SESSION.md for workflow instructions"
    - "Each BOOTSTRAP.md references LEARNINGS.md for shared operational knowledge"
  artifacts:
    - path: "~/clawd/agents/quill/BOOTSTRAP.md"
      provides: "Writer cold-start context"
      contains: "Role"
    - path: "~/clawd/agents/sage/BOOTSTRAP.md"
      provides: "Editor cold-start context"
      contains: "Role"
    - path: "~/clawd/agents/ezra/BOOTSTRAP.md"
      provides: "Publisher cold-start context"
      contains: "Role"
  key_links:
    - from: "~/clawd/agents/quill/BOOTSTRAP.md"
      to: "WRITING_SESSION.md"
      via: "Reference in Working Preferences section"
      pattern: "WRITING_SESSION"
    - from: "~/clawd/agents/sage/BOOTSTRAP.md"
      to: "REVIEW_SESSION.md"
      via: "Reference in Working Preferences section"
      pattern: "REVIEW_SESSION"
    - from: "~/clawd/agents/ezra/BOOTSTRAP.md"
      to: "PUBLISH_SESSION.md"
      via: "Reference in Working Preferences section"
      pattern: "PUBLISH_SESSION"
    - from: "All BOOTSTRAP.md files"
      to: "LEARNINGS.md"
      via: "Reference for shared operational knowledge"
      pattern: "LEARNINGS"
---

<objective>
Create pipeline-specific BOOTSTRAP.md files for content agents so cron-triggered sessions start with prior context.

Purpose: Content agents (Quill, Sage, Ezra) run in isolated cron sessions with zero prior context, producing output disconnected from previous sessions. BOOTSTRAP.md gives each agent enough role, editorial, and pipeline state context for a cold-start session to produce coherent output without DM history.

Output: Three BOOTSTRAP.md files (one per content agent) with role summary, editorial decisions, pipeline state queries, and working preferences.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-memory-retrieval-discipline/35-RESEARCH.md
@.planning/phases/35-memory-retrieval-discipline/35-CONTEXT.md
@.planning/phases/35-memory-retrieval-discipline/35-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing content agent workspace files for context</name>
  <files>
    ~/clawd/agents/quill/WRITING_SESSION.md
    ~/clawd/agents/quill/BOOTSTRAP.md
    ~/clawd/agents/quill/PRODUCT_CONTEXT.md
    ~/clawd/agents/sage/REVIEW_SESSION.md
    ~/clawd/agents/ezra/PUBLISH_SESSION.md
  </files>
  <action>
SSH to EC2 and read the existing workspace files for all three content agents. This provides the source material for BOOTSTRAP.md content:

```bash
ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "echo '=== QUILL BOOTSTRAP ===' && cat ~/clawd/agents/quill/BOOTSTRAP.md && echo '=== QUILL WRITING_SESSION ===' && cat ~/clawd/agents/quill/WRITING_SESSION.md && echo '=== QUILL PRODUCT_CONTEXT (first 30 lines) ===' && head -30 ~/clawd/agents/quill/PRODUCT_CONTEXT.md && echo '=== SAGE REVIEW_SESSION ===' && cat ~/clawd/agents/sage/REVIEW_SESSION.md && echo '=== EZRA PUBLISH_SESSION ===' && cat ~/clawd/agents/ezra/PUBLISH_SESSION.md"
```

Also check if Sage or Ezra already have AGENTS.md or BOOTSTRAP.md:

```bash
ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "ls -la ~/clawd/agents/sage/ && ls -la ~/clawd/agents/ezra/"
```

Also confirm the LEARNINGS.md location from Plan 01 (read the 35-01-SUMMARY.md to know the resolved path).

Use these files to populate agent-specific editorial decisions and working preferences in the BOOTSTRAP.md files. Do NOT duplicate PRODUCT_CONTEXT.md content -- reference it instead.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "ls ~/clawd/agents/quill/WRITING_SESSION.md ~/clawd/agents/sage/REVIEW_SESSION.md ~/clawd/agents/ezra/PUBLISH_SESSION.md 2>&1"</automated>
  </verify>
  <done>All three content agent workspace files read. Source material gathered for BOOTSTRAP.md creation.</done>
</task>

<task type="auto">
  <name>Task 2: Create BOOTSTRAP.md for Quill, Sage, and Ezra</name>
  <files>
    ~/clawd/agents/quill/BOOTSTRAP.md (on EC2 via SSH)
    ~/clawd/agents/sage/BOOTSTRAP.md (on EC2 via SSH)
    ~/clawd/agents/ezra/BOOTSTRAP.md (on EC2 via SSH)
  </files>
  <action>
Create three BOOTSTRAP.md files using the shared template structure from CONTEXT.md decisions. Each file should be 30-50 lines.

**Template structure (per user decision):**
```
# {Agent Name} - Bootstrap Context

## Role
{1-2 sentences: what this agent does in the content pipeline}

## Editorial Decisions
- Content domain: UAS/commercial drones (see PRODUCT_CONTEXT.md for full rules)
- {3-4 agent-specific editorial standing decisions}

## Pipeline State
Check current state at session start:
```sql
{Primary query this agent runs to see what needs doing}
```

## Working Preferences
- {How this agent operates}
- {What to prioritize}
- For shared operational knowledge, check LEARNINGS.md
- For full workflow, follow {SESSION.md}
```

**Agent-specific content:**

**Quill (writer) -- REPLACE existing generic "Hello World" BOOTSTRAP.md:**
- Role: SEO content writer for AirSpace Integration, producing UAS/commercial drone articles
- Editorial: 1500-2500 word target, SEO structure (H2/H3 hierarchy, keyword integration), PRODUCT_CONTEXT.md compliance for domain claims
- Pipeline State SQL: `SELECT id, title, status FROM topics WHERE status = 'backlog' ORDER BY created_at ASC LIMIT 5;` and `SELECT id, title FROM articles WHERE status = 'writing' AND claimed_by = 'quill';`
- Preferences: Check for in-progress articles before claiming new topics, avoid similar topics back-to-back, one article per session maximum

**Sage (editor) -- CREATE new:**
- Role: Editorial reviewer scoring articles for SEO quality, readability, and factual accuracy
- Editorial: Three-dimensional scoring (SEO/readability/accuracy 1-10), approval threshold >= 7 on all three, PRODUCT_CONTEXT.md as compliance baseline for UAS claims
- Pipeline State SQL: `SELECT a.id, a.title, a.status FROM articles a WHERE a.status = 'review' ORDER BY a.updated_at ASC LIMIT 5;`
- Preferences: Verify factual claims via web search when possible, document unverifiable claims as reviewer notes, append new reviews to existing reviewer_notes to preserve history

**Ezra (publisher) -- CREATE new:**
- Role: WordPress publisher creating WP drafts and generating social media copy for approved articles
- Editorial: Draft-only publishing (human approves in WP Admin), social copy for LinkedIn/X/Instagram per article, check for human-published confirmations
- Pipeline State SQL: `SELECT a.id, a.title FROM articles a WHERE a.status = 'approved' ORDER BY a.updated_at ASC LIMIT 3;` and `SELECT a.id, a.title, a.wp_post_id FROM articles a WHERE a.status = 'published' AND a.wp_post_id IS NOT NULL ORDER BY a.updated_at DESC LIMIT 3;`
- Preferences: Process up to 3 articles per session, check for human-published articles to update status, generate social copy only for articles with confirmed WP publication

**Critical anti-patterns to avoid (from research):**
- Do NOT embed specific article IDs/titles in Pipeline State -- use SQL queries so content is always current
- Do NOT copy PRODUCT_CONTEXT.md content -- reference it
- Do NOT leave any section empty or with [TODO] placeholders -- every field must have actual content
- BOOTSTRAP.md says "who you are and what you know"; SESSION.md says "what to do right now" -- reference the session doc, don't duplicate its workflow

Write each file via SSH using tee. Back up Quill's existing BOOTSTRAP.md first:

```bash
ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cp ~/clawd/agents/quill/BOOTSTRAP.md ~/clawd/agents/quill/BOOTSTRAP.md.bak-$(date +%Y%m%d)"
```
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "for agent in quill sage ezra; do echo \"=== \$agent ===\"; wc -l ~/clawd/agents/\$agent/BOOTSTRAP.md; grep -c 'Role\|Editorial\|Pipeline State\|Working Preferences' ~/clawd/agents/\$agent/BOOTSTRAP.md; done"</automated>
    <manual>Verify each BOOTSTRAP.md has all 4 sections populated with real content (not templates/placeholders), references its SESSION.md, references LEARNINGS.md, and references PRODUCT_CONTEXT.md without duplicating it</manual>
  </verify>
  <done>Three BOOTSTRAP.md files created: Quill's generic template replaced with writer context, Sage and Ezra have new editor/publisher context. Each file is 30-50 lines with Role, Editorial Decisions, Pipeline State (SQL queries), and Working Preferences sections. All reference their respective SESSION.md for workflow and LEARNINGS.md for operational knowledge.</done>
</task>

</tasks>

<verification>
1. Quill's BOOTSTRAP.md no longer contains "Hello World" or "woke up" generic template text
2. All three BOOTSTRAP.md files exist with 30-50 lines each
3. Each file has all 4 sections: Role, Editorial Decisions, Pipeline State, Working Preferences
4. Each Pipeline State section contains actual SQL queries (not placeholders)
5. Each Working Preferences references the agent's SESSION.md file
6. Each BOOTSTRAP.md references LEARNINGS.md for shared operational knowledge
7. No PRODUCT_CONTEXT.md content duplicated (only referenced)
</verification>

<success_criteria>
- Quill's BOOTSTRAP.md replaced with SEO writer context (no generic template)
- Sage has new BOOTSTRAP.md with editorial review scoring context
- Ezra has new BOOTSTRAP.md with WP publishing and social copy context
- All three reference PRODUCT_CONTEXT.md, LEARNINGS.md, and their respective SESSION.md
- Each file is 30-50 lines with no empty sections or TODO placeholders
- A cold-start cron session for any content agent would have enough context to produce coherent output
</success_criteria>

<output>
After completion, create `.planning/phases/35-memory-retrieval-discipline/35-02-SUMMARY.md`
</output>
