---
phase: 51-compaction-config-qmd-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [COMP-01, COMP-02, SRCH-01, SRCH-02]

must_haves:
  truths:
    - "openclaw.json contains softThresholdTokens=8000 and reserveTokensFloor=40000"
    - "openclaw.json contains vectorWeight=0.7 and textWeight=0.3 in memorySearch"
    - "QMD collections are bootstrapped with embeddings for all 3 auto-created collections"
    - "openclaw.json.bak exists as rollback safety net"
  artifacts:
    - path: "~/.openclaw/openclaw.json"
      provides: "Updated compaction + search config"
      contains: "softThresholdTokens.*8000"
    - path: "~/.openclaw/openclaw.json.bak"
      provides: "Pre-edit backup for rollback"
  key_links:
    - from: "openclaw.json compaction config"
      to: "gateway compaction behavior"
      via: "agents.defaults.compaction"
      pattern: "softThresholdTokens.*8000"
    - from: "openclaw.json memorySearch config"
      to: "QMD hybrid search behavior"
      via: "agents.defaults.memorySearch"
      pattern: "vectorWeight.*0\\.7"
---

<objective>
Apply all config changes to openclaw.json (compaction thresholds + search weights) and bootstrap QMD collections with embeddings. This is all pre-restart work — gateway restart happens in Plan 02.

Purpose: Get the config file correct and QMD ready before the single gateway restart.
Output: Updated openclaw.json with correct values, .bak backup, bootstrapped QMD collections.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-compaction-config-qmd-bootstrap/51-CONTEXT.md
</context>

<rollback>
If config edit goes wrong: `cp ~/.openclaw/openclaw.json.bak ~/.openclaw/openclaw.json` on EC2.
If QMD bootstrap OOMs: `systemctl --user stop mission-control.service` to free memory, retry QMD commands, then `systemctl --user start mission-control.service`.
QMD bootstrap is independent of openclaw.json — QMD persists its own index files.
</rollback>

<tasks>

<task type="auto">
  <name>Task 1: Backup and update openclaw.json with compaction + search config</name>
  <files>~/.openclaw/openclaw.json (on EC2 via SSH)</files>
  <action>
All commands run via SSH to ubuntu@100.72.143.9 using key ~/.ssh/clawdbot-key.pem.

1. Create backup:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cp ~/.openclaw/openclaw.json ~/.openclaw/openclaw.json.bak"
   ```

2. Read current openclaw.json to understand structure:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cat ~/.openclaw/openclaw.json"
   ```

3. Update compaction config — set these values under `agents.defaults.compaction`:
   - `softThresholdTokens`: 8000 (was 1500)
   - `reserveTokensFloor`: 40000 (was 24000)
   Use jq for atomic JSON editing:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "
     jq '.agents.defaults.compaction.softThresholdTokens = 8000 | .agents.defaults.compaction.reserveTokensFloor = 40000' ~/.openclaw/openclaw.json > /tmp/oc-temp.json && mv /tmp/oc-temp.json ~/.openclaw/openclaw.json
   "
   ```

4. Update memorySearch config — set hybrid search weights under `agents.defaults.memorySearch`:
   - `vectorWeight`: 0.7
   - `textWeight`: 0.3
   Use jq:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "
     jq '.agents.defaults.memorySearch.vectorWeight = 0.7 | .agents.defaults.memorySearch.textWeight = 0.3' ~/.openclaw/openclaw.json > /tmp/oc-temp.json && mv /tmp/oc-temp.json ~/.openclaw/openclaw.json
   "
   ```

5. Verify all 4 values are set correctly:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "
     jq '{
       softThresholdTokens: .agents.defaults.compaction.softThresholdTokens,
       reserveTokensFloor: .agents.defaults.compaction.reserveTokensFloor,
       vectorWeight: .agents.defaults.memorySearch.vectorWeight,
       textWeight: .agents.defaults.memorySearch.textWeight
     }' ~/.openclaw/openclaw.json
   "
   ```
   Expected output: `{ "softThresholdTokens": 8000, "reserveTokensFloor": 40000, "vectorWeight": 0.7, "textWeight": 0.3 }`

6. Verify JSON is valid (jq will error on malformed JSON):
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "jq empty ~/.openclaw/openclaw.json && echo 'JSON valid'"
   ```

IMPORTANT: The jq paths above assume the config structure has `agents.defaults.compaction` and `agents.defaults.memorySearch` at the top level. After reading the file in step 2, adjust paths if the actual structure differs. The CONTEXT.md confirms these paths.

If jq is not installed on EC2, fall back to python3:
```
python3 -c "import json; c=json.load(open('/home/ubuntu/.openclaw/openclaw.json')); c['agents']['defaults']['compaction']['softThresholdTokens']=8000; ..."
```
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "jq '{soft: .agents.defaults.compaction.softThresholdTokens, reserve: .agents.defaults.compaction.reserveTokensFloor, vw: .agents.defaults.memorySearch.vectorWeight, tw: .agents.defaults.memorySearch.textWeight}' ~/.openclaw/openclaw.json" | grep -q '"soft": 8000' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>openclaw.json contains softThresholdTokens=8000, reserveTokensFloor=40000, vectorWeight=0.7, textWeight=0.3. Backup exists at openclaw.json.bak. JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap QMD collections with update and embed</name>
  <files>~/.openclaw/agents/main/qmd/ (on EC2 via SSH)</files>
  <action>
All commands run via SSH to ubuntu@100.72.143.9.

1. Check current QMD collection status — list what exists:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "ls -la ~/.openclaw/agents/main/qmd/ 2>/dev/null; ~/.bun/bin/qmd status 2>/dev/null || echo 'qmd status not available'"
   ```

2. Check available memory before bootstrap (QMD embedding loads ~1.5GB models):
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "free -h"
   ```
   If available memory is below 500MB, stop mission-control first:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "systemctl --user stop mission-control.service"
   ```

3. Run QMD update to scan and index files for all collections:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd update 2>&1"
   ```
   This should scan memory-root, memory-alt, and memory-dir collections.

4. Run QMD embed to generate vector embeddings:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd embed 2>&1"
   ```
   This may take 2-5 minutes on t3.small. Use a longer SSH timeout if needed.

5. Verify QMD search returns results — test with known content:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'Andy' 2>&1"
   ```
   Should return non-empty results from at least memory-dir-main collection.

6. Test additional queries per CONTEXT.md decision (mix of system knowledge + recent events):
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'content pipeline' 2>&1"
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'mission control' 2>&1"
   ```

7. If mission-control was stopped in step 2, restart it:
   ```
   ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "systemctl --user start mission-control.service && systemctl --user is-active mission-control.service"
   ```

NOTE: QMD commands must be run from the agent directory (`~/.openclaw/agents/main`) or with the correct QMD_HOME set. The QMD home directory is `~/.openclaw/agents/main/qmd/`.

If `qmd update` or `qmd embed` OOMs, stop mission-control.service and Docker containers (`docker stop $(docker ps -q)`) to free memory, then retry.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "cd ~/.openclaw/agents/main && ~/.bun/bin/qmd search 'Andy' 2>&1 | head -20" | grep -q -v "^$" && echo "PASS: QMD returns results" || echo "FAIL: QMD returned empty"</automated>
  </verify>
  <done>QMD update + embed completed for all 3 collections (memory-root, memory-alt, memory-dir). `qmd search "Andy"` returns non-empty results. Mission-control service is running if it was stopped.</done>
</task>

</tasks>

<verification>
1. `openclaw.json.bak` exists on EC2
2. `jq .agents.defaults.compaction.softThresholdTokens` returns 8000
3. `jq .agents.defaults.compaction.reserveTokensFloor` returns 40000
4. `jq .agents.defaults.memorySearch.vectorWeight` returns 0.7
5. `jq .agents.defaults.memorySearch.textWeight` returns 0.3
6. `qmd search "Andy"` returns non-empty results
7. JSON file passes `jq empty` validation
</verification>

<success_criteria>
- All 4 config values are correct in openclaw.json
- Backup .bak exists for rollback
- QMD collections bootstrapped with embeddings
- QMD search queries return meaningful results
- No services are left in a stopped state
</success_criteria>

<output>
After completion, create `.planning/phases/51-compaction-config-qmd-bootstrap/51-01-SUMMARY.md`
</output>
