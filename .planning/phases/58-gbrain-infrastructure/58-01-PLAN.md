---
phase: 58-gbrain-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "EC2: ~/.bun/bin/bun (Bun runtime)"
  - "EC2: ~/gbrain/ (gbrain repo clone)"
  - "EC2: ~/.bun/bin/gbrain (symlink via bun link)"
  - "EC2: ~/clawd/db/gbrain/brain.pglite/ (PGLite database)"
  - "EC2: ~/.gbrain/config.json (gbrain config)"
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "gbrain --version returns v0.10.1 (or current version) on the EC2 host"
    - "gbrain doctor passes DB-related checks on the host"
    - "PGLite database exists at ~/clawd/db/gbrain/brain.pglite/"
    - "~/.gbrain/config.json exists with engine=pglite and correct database_path"
  artifacts:
    - path: "EC2: ~/.bun/bin/bun"
      provides: "Bun runtime required by gbrain"
    - path: "EC2: ~/gbrain/"
      provides: "gbrain source repo (needed for bun link and potential Path B)"
    - path: "EC2: ~/clawd/db/gbrain/brain.pglite/"
      provides: "PGLite embedded database directory"
    - path: "EC2: ~/.gbrain/config.json"
      provides: "gbrain configuration pointing to PGLite"
  key_links:
    - from: "~/.bun/bin/gbrain"
      to: "~/gbrain/src/cli.ts"
      via: "bun link symlink"
      pattern: "gbrain -> .*/gbrain/src/cli.ts"
    - from: "~/.gbrain/config.json"
      to: "~/clawd/db/gbrain/brain.pglite"
      via: "database_path field"
      pattern: "database_path.*clawd/db/gbrain"
---

<objective>
Install Bun runtime and gbrain CLI on the EC2 host, initialize PGLite database at the canonical data directory, and verify gbrain is functional on the host before configuring sandbox access.

Purpose: INFRA-01 requires gbrain CLI installed on EC2 with PGLite engine. This is the foundation -- sandbox bind-mounting (Plan 02) depends on a working host installation.
Output: Working gbrain CLI on EC2 host with initialized PGLite database at ~/clawd/db/gbrain/brain.pglite/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-gbrain-infrastructure/58-CONTEXT.md
@.planning/phases/58-gbrain-infrastructure/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Bun and gbrain on EC2 host</name>
  <files>
    EC2: ~/.bun/bin/bun
    EC2: ~/gbrain/
    EC2: ~/.bun/bin/gbrain
    EC2: ~/.bashrc (PATH addition)
  </files>
  <read_first>
    .planning/phases/58-gbrain-infrastructure/58-RESEARCH.md (installation commands, two-path strategy)
    .planning/phases/58-gbrain-infrastructure/58-CONTEXT.md (decisions D-07 corrected to Bun, D-08, D-09)
  </read_first>
  <action>
SSH to EC2: `ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9`

All commands run on EC2 via SSH.

Step 1 -- Install Bun:
```bash
curl -fsSL https://bun.sh/install | bash
export PATH="$HOME/.bun/bin:$PATH"
```

Verify `~/.bashrc` already has the PATH export (the installer usually adds it). If not, append:
```bash
echo 'export PATH="$HOME/.bun/bin:$PATH"' >> ~/.bashrc
```

Verify: `bun --version` should return 1.2.x or higher.

Step 2 -- Clone and link gbrain:
```bash
git clone https://github.com/garrytan/gbrain.git ~/gbrain
cd ~/gbrain && bun install && bun link
```

Verify: `gbrain --version` should return a version string (expected: 0.10.1).

Step 3 -- Attempt compiled binary (Path A per research, D-07 spirit of global CLI):
```bash
cd ~/gbrain
bun build --compile --target=bun-linux-x64 --outfile bin/gbrain-linux-x64 src/cli.ts
```

Test the compiled binary:
```bash
~/gbrain/bin/gbrain-linux-x64 --version
```

If it prints the version without error, note "Path A succeeded" for Plan 02.
If it fails with ENOENT containing `$bunfs/root/postgres.data` or similar WASM error, note "Path A failed, using Path B (Bun runtime)" for Plan 02. Do NOT spend time debugging -- the research documents this as a known open bug (electric-sql/pglite#414).

Per D-07 (corrected by research): gbrain requires Bun, not npm. Same spirit -- global CLI install on host.
Per D-09: If gbrain has runtime deps, they're handled by the bun install step.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 'export PATH="$HOME/.bun/bin:$PATH" && bun --version && gbrain --version'</automated>
  </verify>
  <acceptance_criteria>
    - `bun --version` returns a version >= 1.2.0
    - `gbrain --version` returns a version string (e.g., "gbrain 0.10.1" or "0.10.1")
    - ~/gbrain/ directory exists with node_modules/ populated
    - ~/.bun/bin/gbrain symlink exists
    - Result of compiled binary attempt is documented (Path A success or Path B needed)
  </acceptance_criteria>
  <done>Bun and gbrain are installed on EC2 host. gbrain --version returns a valid version. Compiled binary status is known.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize PGLite database and configure gbrain</name>
  <files>
    EC2: ~/clawd/db/gbrain/brain.pglite/ (PGLite database directory)
    EC2: ~/.gbrain/config.json (gbrain configuration)
  </files>
  <read_first>
    .planning/phases/58-gbrain-infrastructure/58-RESEARCH.md (init command, config.json layout, data directory pattern, pitfall 2 about config path mismatch)
    .planning/phases/58-gbrain-infrastructure/58-CONTEXT.md (D-06: data dir at ~/clawd/db/gbrain/, D-10: text-embedding-3-small)
  </read_first>
  <action>
SSH to EC2: `ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9`

All commands run on EC2 via SSH.

Step 1 -- Create data directory following established ~/clawd/db/ pattern (per D-06):
```bash
mkdir -p ~/clawd/db/gbrain
```

Step 2 -- Initialize PGLite database:
The OpenAI API key is needed for init. Check if Andy has it set or if it needs to be provided. If `OPENAI_API_KEY` is already in the environment or Andy provides it, use it. Otherwise, init without the key (can be added later via env var).

Run init with the custom data path:
```bash
export PATH="$HOME/.bun/bin:$PATH"
gbrain init --pglite --path ~/clawd/db/gbrain/brain.pglite --non-interactive
```

If init prompts for an API key and `--key` flag is supported:
```bash
gbrain init --pglite --path ~/clawd/db/gbrain/brain.pglite --key "$OPENAI_API_KEY" --non-interactive
```

Step 3 -- Verify config.json was created:
```bash
cat ~/.gbrain/config.json
```
Expected contents should include:
- `"engine": "pglite"` (or equivalent key)
- `"database_path"` pointing to the PGLite directory

Step 4 -- Create a HOST-SIDE config for host verification AND a SANDBOX config for sandbox use.

CRITICAL (Pitfall 2 from research): The config.json created by `gbrain init` will contain HOST paths (e.g., `/home/ubuntu/clawd/db/gbrain/brain.pglite`). Inside the sandbox, the path is different (`/workspace/db/gbrain/brain.pglite`). 

Strategy: Keep the host config as-is for host-side verification. In Plan 02, we will create a sandbox-specific config at `~/.gbrain/sandbox-config.json` with sandbox paths, or handle the path translation via bind-mount or env var.

For now, just verify the host config works.

Step 5 -- Run doctor on host:
```bash
gbrain doctor
```

Expected: DB-related checks (DB connection, pgvector, schema version) should pass. Skill-related checks (resolver, skill conformance) may warn -- that is acceptable since Bob uses BRAIN_OPS.md protocol, not gbrain skills.

Step 6 -- Smoke test -- create a test page and verify search works:
```bash
echo '---
type: test
title: Installation Verification
tags: [test, setup]
---

This is a test page created during gbrain installation verification on the pops-claw EC2 instance.' | gbrain put test-install-verify

gbrain search "installation verification"
```

Verify search returns the test page.

Per D-06: Data directory at ~/clawd/db/gbrain/ on host.
Per D-10: text-embedding-3-small for embeddings (configured via OpenAI API key, gbrain defaults to this model).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 'export PATH="$HOME/.bun/bin:$PATH" && test -d ~/clawd/db/gbrain/brain.pglite && cat ~/.gbrain/config.json && gbrain doctor && gbrain search "installation verification"'</automated>
  </verify>
  <acceptance_criteria>
    - ~/clawd/db/gbrain/brain.pglite/ directory exists and contains PG_VERSION file
    - ~/.gbrain/config.json exists and contains "pglite" (engine type)
    - ~/.gbrain/config.json contains a database_path pointing to brain.pglite
    - `gbrain doctor` output shows DB connection check passing (health score >= 60, allowing skill warnings)
    - `gbrain search "installation verification"` returns at least one result containing "test-install-verify"
  </acceptance_criteria>
  <done>PGLite database initialized at ~/clawd/db/gbrain/brain.pglite/. Config points to correct path. Doctor passes DB checks. Test page created and searchable.</done>
</task>

</tasks>

<verification>
Host-side gbrain installation is complete and functional:
1. `bun --version` returns >= 1.2.0
2. `gbrain --version` returns version string
3. `gbrain doctor` passes DB checks (health score >= 60)
4. Test page exists and is searchable via `gbrain search`
5. PGLite data lives at ~/clawd/db/gbrain/ (canonical db directory)
6. Compiled binary status documented for Plan 02 to consume
</verification>

<success_criteria>
- gbrain CLI is installed and callable from EC2 host
- PGLite database is initialized at the canonical path ~/clawd/db/gbrain/brain.pglite/
- gbrain doctor passes database checks
- A test page can be created and searched
- The compiled binary attempt result is documented (Path A or Path B needed for sandbox)
</success_criteria>

<output>
After completion, create `.planning/phases/58-gbrain-infrastructure/58-01-SUMMARY.md`

CRITICAL: Document whether the compiled binary (Path A) worked or failed. Plan 02 depends on this to configure the correct bind-mount strategy.
</output>
