---
phase: 56-goals-journal
plan: 02
type: execute
wave: 2
depends_on: [56-01]
target_repo: andyos-dashboard
files_modified:
  - src/app/(dashboard)/goals/page.tsx (create)
  - src/app/(dashboard)/goals/loading.tsx (create)
  - src/app/(dashboard)/goals/error.tsx (create)
  - src/app/(dashboard)/journal/page.tsx (create)
  - src/app/(dashboard)/journal/loading.tsx (create)
  - src/app/(dashboard)/journal/error.tsx (create)
  - src/components/goals/ (create directory)
  - src/components/journal/ (create directory)
  - src/components/hub/goals-card.tsx (create)
  - src/components/hub/journal-card.tsx (create)
  - src/components/dashboard-shell.tsx (add nav items)
  - src/app/(dashboard)/overview/page.tsx (add hub cards)
autonomous: true
requirements: [GOAL-01, GOAL-02, GOAL-03, GOAL-04, JRNL-01, JRNL-02, JRNL-03, JRNL-04]

must_haves:
  truths:
    - "/goals page shows active goals with progress bars and key results"
    - "/goals page has create goal form with objective + key results"
    - "/goals page supports check-in, complete, and abandon actions"
    - "/journal page shows today's prompt with response textarea"
    - "/journal page has inline mood (1-5) and energy (1-5) selectors"
    - "/journal page shows past entries timeline with mood/energy"
    - "Hub has Goals card linking to /goals"
    - "Hub has Journal card linking to /journal"
    - "Sidebar nav has Goals and Journal items in LIFE section"
  artifacts:
    - path: "src/app/(dashboard)/goals/page.tsx"
      provides: "Goals list + create + manage page"
    - path: "src/app/(dashboard)/journal/page.tsx"
      provides: "Journal prompt + entry + history page"
    - path: "src/components/hub/goals-card.tsx"
      provides: "Hub card showing active goals and lowest-progress goal"
    - path: "src/components/hub/journal-card.tsx"
      provides: "Hub card showing today's prompt and streak"
---

<objective>
Build the Goals and Journal UI pages, hub cards, and nav integration in andyOS Dashboard.

Purpose: Full user-facing experience for goal tracking and journaling.
Output: /goals page, /journal page, 2 hub cards, sidebar nav items. All following existing dashboard design patterns.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-goals-journal/56-CONTEXT.md
@.planning/phases/56-goals-journal/56-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Goals page and components</name>
  <files>src/app/(dashboard)/goals/, src/components/goals/ in andyos-dashboard</files>
  <read_first>
    - src/app/(dashboard)/supplements/page.tsx (existing page pattern: layout, SWR fetching, Card structure)
    - src/components/dashboard-shell.tsx (nav structure, LIFE section)
    - src/lib/schema.ts (goal table shape from Plan 01)
    - src/app/api/goals/route.ts (API contract from Plan 01)
    - src/components/hub/health-card.tsx (hub card Suspense pattern)
  </read_first>
  <action>
Working directory: `/Users/andykaufman/Desktop/Projects/andyos-dashboard`

Create the goals page following existing dashboard patterns (shadcn cards, Tailwind dark mode, same layout structure as /projects or /supplements pages).

**Page structure (`src/app/(dashboard)/goals/page.tsx`):**

1. **Header**: "Goals" title + "New Goal" button (opens create dialog/sheet)
2. **Active Goals List**: Cards for each active goal showing:
   - Goal title + category badge
   - Overall progress bar (colored: red <30%, yellow 30-70%, green >70%)
   - Key results list with individual progress indicators
   - Target date with days remaining
   - Actions: check-in, complete, abandon (via dropdown menu)
3. **Empty state**: "No active goals. Set your first goal to start tracking progress."
4. **Completed/Abandoned toggle**: Tab or filter to view past goals

**Components to create in `src/components/goals/`:**

1. `goal-card.tsx` — Single goal display with progress bar and KR list
2. `create-goal-dialog.tsx` — Sheet/dialog with form:
   - Objective (text input)
   - Key results: dynamic list (1-3 inputs with + button)
   - Category dropdown (health, career, learning, financial, personal)
   - Target date (date picker, optional)
3. `checkin-dialog.tsx` — Quick check-in sheet:
   - KR progress sliders or number inputs (0-100%)
   - Notes textarea
   - Confidence 1-5 rating
4. `goal-progress-bar.tsx` — Reusable colored progress bar component

**Follow existing patterns:**
- Use SWR for client-side data fetching (see how /supplements or /tasks does it)
- Optimistic updates on check-in
- shadcn Dialog/Sheet for create/edit forms
- Same Card/CardHeader/CardContent structure as other pages
- Loading state: skeleton cards (create loading.tsx)
- Error boundary: create error.tsx
  </action>
  <verify>
    <automated>cd /Users/andykaufman/Desktop/Projects/andyos-dashboard && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/app/\(dashboard\)/goals/page.tsx
    - test -f src/app/\(dashboard\)/goals/loading.tsx
    - test -f src/app/\(dashboard\)/goals/error.tsx
    - test -f src/components/goals/goal-card.tsx
    - test -f src/components/goals/create-goal-dialog.tsx
    - test -f src/components/goals/checkin-dialog.tsx
    - test -f src/components/goals/goal-progress-bar.tsx
    - grep -q "api/goals" src/app/\(dashboard\)/goals/page.tsx
    - grep -q "progress" src/components/goals/goal-card.tsx
  </acceptance_criteria>
  <done>Goals page with create dialog, goal cards with progress bars, check-in dialog, and complete/abandon actions. Follows existing dashboard patterns (shadcn, SWR, dark mode).</done>
</task>

<task type="auto">
  <name>Task 2: Create Journal page and components</name>
  <files>src/app/(dashboard)/journal/, src/components/journal/ in andyos-dashboard</files>
  <read_first>
    - src/app/(dashboard)/goals/page.tsx (page layout pattern from Task 1)
    - src/lib/schema.ts (journalEntry table shape from Plan 01)
    - src/app/api/journal/route.ts (journal API contract from Plan 01)
    - src/app/api/journal/prompt/route.ts (prompt API contract from Plan 01)
    - src/app/api/journal/stats/route.ts (stats API contract from Plan 01)
  </read_first>
  <action>
Working directory: `/Users/andykaufman/Desktop/Projects/andyos-dashboard`

**Page structure (`src/app/(dashboard)/journal/page.tsx`):**

1. **Today's Prompt Section** (top of page):
   - Category label (e.g., "Monday — Gratitude")
   - Prompt text in larger font
   - Textarea for response (auto-saves or explicit save button)
   - Inline mood selector: 5 buttons (1-5, could use emoji or numbers)
   - Inline energy selector: 5 buttons (1-5)
   - "Save Entry" button
   - If already journaled today: show saved response with edit option

2. **Stats Bar** (below prompt):
   - Streak count (flame icon)
   - Completion rate (% over last 30 days)
   - Average mood + average energy

3. **Past Entries Timeline** (below stats):
   - List of recent entries (last 7-14 days)
   - Each shows: date, category badge, first ~100 chars of response, mood/energy dots
   - Click to expand full entry
   - "Load more" for older entries

**Components to create in `src/components/journal/`:**

1. `journal-prompt.tsx` — Today's prompt display with response form
2. `mood-energy-selector.tsx` — Inline 1-5 selector (reusable for both mood and energy)
   - 5 clickable circles/buttons, selected one highlighted
   - Labels: mood uses "Bad -> Great", energy uses "Low -> High"
3. `journal-stats.tsx` — Stats bar with streak, completion rate, averages
4. `journal-timeline.tsx` — Past entries list with expand/collapse
5. `journal-entry-card.tsx` — Single past entry display

**Design specifics:**
- Mood/energy selectors should be quick to tap (not dropdowns) — think radio buttons styled as pills
- Past entries use muted colors, current day prompt is visually prominent
- Category badges use subtle colored backgrounds matching the theme
- Same dark mode treatment as rest of dashboard
  </action>
  <verify>
    <automated>cd /Users/andykaufman/Desktop/Projects/andyos-dashboard && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/app/\(dashboard\)/journal/page.tsx
    - test -f src/app/\(dashboard\)/journal/loading.tsx
    - test -f src/app/\(dashboard\)/journal/error.tsx
    - test -f src/components/journal/journal-prompt.tsx
    - test -f src/components/journal/mood-energy-selector.tsx
    - test -f src/components/journal/journal-stats.tsx
    - test -f src/components/journal/journal-timeline.tsx
    - grep -q "api/journal" src/app/\(dashboard\)/journal/page.tsx
    - grep -q "mood" src/components/journal/mood-energy-selector.tsx
  </acceptance_criteria>
  <done>Journal page with today's prompt, mood/energy selectors, stats bar, and past entries timeline. Clean, quick-entry focused design.</done>
</task>

<task type="auto">
  <name>Task 3: Add hub cards and sidebar nav</name>
  <files>src/components/hub/goals-card.tsx, src/components/hub/journal-card.tsx, src/components/dashboard-shell.tsx, src/app/(dashboard)/overview/page.tsx in andyos-dashboard</files>
  <read_first>
    - src/components/hub/health-card.tsx (exact hub card pattern: async RSC, Suspense, direct DB query)
    - src/lib/data/hub.ts (existing hub data functions)
    - src/components/dashboard-shell.tsx (navSections structure, LIFE section, moreSheetSections)
    - src/app/(dashboard)/overview/page.tsx (existing hub card grid layout)
  </read_first>
  <action>
Working directory: `/Users/andykaufman/Desktop/Projects/andyos-dashboard`

**1. Goals Hub Card (`src/components/hub/goals-card.tsx`)**

Follow the exact pattern from `health-card.tsx`:
- Async RSC with Suspense wrapper
- Direct DB query (not API call) in `src/lib/data/hub.ts`
- Shows: active goal count, lowest-progress goal name + percentage
- Empty state: "No goals set" with link to /goals
- Links to /goals on click

**2. Journal Hub Card (`src/components/hub/journal-card.tsx`)**

Same pattern:
- Shows: today's prompt category, streak count, whether already journaled today
- If journaled: show mood/energy from today's entry
- If not: show prompt preview with "Reflect ->" CTA
- Links to /journal on click

**3. Add data functions to `src/lib/data/hub.ts`:**

```typescript
export async function getGoalsSummary(userId: string) {
  // Count active goals, find lowest-progress goal
}

export async function getJournalSummary(userId: string) {
  // Today's entry status, streak count, prompt category
}
```

**4. Update hub overview page** (`src/app/(dashboard)/overview/page.tsx`):
- Add GoalsCard and JournalCard to the grid
- Same Suspense wrapping as existing cards

**5. Update sidebar nav** (`src/components/dashboard-shell.tsx`):

Add to the LIFE section:
```typescript
import { Target, BookOpen } from "lucide-react";

// In navSections, LIFE items:
{ label: "Goals", href: "/goals", icon: Target },
{ label: "Journal", href: "/journal", icon: BookOpen },
```

Also add to `moreSheetSections` LIFE items for mobile.
  </action>
  <verify>
    <automated>cd /Users/andykaufman/Desktop/Projects/andyos-dashboard && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/components/hub/goals-card.tsx
    - test -f src/components/hub/journal-card.tsx
    - grep -q "Goals" src/components/dashboard-shell.tsx
    - grep -q "Journal" src/components/dashboard-shell.tsx
    - grep -q "Target" src/components/dashboard-shell.tsx
    - grep -q "BookOpen" src/components/dashboard-shell.tsx
    - grep -q "GoalsCard\|goals-card" src/app/\(dashboard\)/overview/page.tsx
    - grep -q "JournalCard\|journal-card" src/app/\(dashboard\)/overview/page.tsx
    - grep -q "getGoalsSummary" src/lib/data/hub.ts
    - grep -q "getJournalSummary" src/lib/data/hub.ts
  </acceptance_criteria>
  <done>Hub cards for Goals and Journal added to overview page with Suspense streaming. Sidebar nav updated with Goals and Journal in LIFE section. Mobile "More" sheet also updated.</done>
</task>

</tasks>

<verification>
- /goals page renders with active goals, progress bars, create/check-in/complete/abandon
- /journal page renders with today's prompt, mood/energy selectors, past entries
- Hub shows Goals card and Journal card with live data
- Sidebar nav has Goals (Target icon) and Journal (BookOpen icon) in LIFE section
- Mobile bottom bar "More" sheet includes both new items
- TypeScript compiles cleanly
- All components follow existing shadcn/dark mode patterns
- Loading and error states work
</verification>

<success_criteria>
Full UI experience deployed: users can create goals with key results, track progress visually, write journal entries with mood/energy, see summaries on the hub, and navigate via sidebar. All matching existing andyOS design patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/56-goals-journal/56-02-SUMMARY.md`
</output>
