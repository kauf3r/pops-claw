---
phase: 49-install-top-5-clawhhub-skills-on-ec2
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - ~/.openclaw/.env
  - ~/.openclaw/openclaw.json
autonomous: false
requirements:
  - ENV-CONFIG
  - GATEWAY-VERIFY

user_setup:
  - service: TranscriptAPI
    why: "youtube-full skill needs transcript extraction API"
    env_vars:
      - name: TRANSCRIPT_API_KEY
        source: "https://transcriptapi.com — sign up (free tier: 100 credits/month), get API key from dashboard"
    dashboard_config: []
  - service: Apify
    why: "real-estate-lead-machine skill uses Apify actors for property scraping"
    env_vars:
      - name: APIFY_API_TOKEN
        source: "https://apify.com — sign up (free tier: $5/month credits, no card required), get token from Settings → Integrations"
    dashboard_config: []

must_haves:
  truths:
    - "Gateway restarts successfully and loads all new skills"
    - "openclaw doctor shows no critical issues for newly installed skills"
    - "Environment variables for external services are configured or have placeholder values"
    - "User has been informed of any API keys they need to provide"
  artifacts:
    - path: "~/.openclaw/.env"
      provides: "Environment variables for skill API keys"
      contains: "APIFY_API_TOKEN"
    - path: "~/.openclaw/openclaw.json"
      provides: "Skill configuration entries"
  key_links:
    - from: "~/.openclaw/.env"
      to: "openclaw-gateway.service"
      via: "EnvironmentFile directive in systemd unit"
      pattern: "EnvironmentFile"
    - from: "~/.openclaw/openclaw.json"
      to: "skill eligibility"
      via: "gateway reads config at startup"
      pattern: "skills.entries"
---

<objective>
Configure environment variables, restart the gateway, and verify all skills are recognized.

Purpose: Complete the installation by ensuring the gateway loads the new skills and all required configuration is in place.
Output: Running gateway with all 5 skills recognized. User informed of any external API keys to provide.
</objective>

<execution_context>
@/Users/andykaufman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andykaufman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-install-top-5-clawhhub-skills-on-ec2/49-RESEARCH.md
@.planning/phases/49-install-top-5-clawhhub-skills-on-ec2/49-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure environment variables and openclaw.json</name>
  <files>~/.openclaw/.env, ~/.openclaw/openclaw.json</files>
  <action>
SSH to EC2 (ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9) and configure:

1. **Check existing env vars:**
   ```bash
   grep -E 'TRANSCRIPT_API_KEY|GEMINI_API_KEY|APIFY_API_TOKEN' ~/.openclaw/.env
   ```

2. **GEMINI_API_KEY** -- should already be set (used for memory embeddings). Verify it exists. If missing, this is a problem -- flag it.

3. **TRANSCRIPT_API_KEY** -- check if set. If NOT set:
   - Add placeholder: `echo 'TRANSCRIPT_API_KEY=NEEDS_USER_INPUT' >> ~/.openclaw/.env`
   - Note: youtube-full won't work for transcript extraction without this key

4. **APIFY_API_TOKEN** -- this is new, definitely not set:
   - Add placeholder: `echo 'APIFY_API_TOKEN=NEEDS_USER_INPUT' >> ~/.openclaw/.env`
   - Note: real-estate-lead-machine won't work without Apify account/token

5. **Check openclaw.json for skill entries:**
   ```bash
   cat ~/.openclaw/openclaw.json | python3 -c "import sys,json; c=json.load(sys.stdin); print(json.dumps(c.get('skills',{}).get('entries',{}), indent=2))"
   ```
   - nano-banana-pro should already have an apiKey entry (Gemini key)
   - If missing, add it under skills.entries in openclaw.json

6. **Check sandbox env vars** -- if any new env vars need to be available inside the Docker sandbox, add them to `agents.defaults.sandbox.docker.env` in openclaw.json. Check which skills are sandbox-only vs host-side by reading their SKILL.md files.

Do NOT overwrite existing values. Only add missing entries. Use `>>` (append) not `>` (overwrite) for .env changes.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "grep -c 'GEMINI_API_KEY\|TRANSCRIPT_API_KEY\|APIFY_API_TOKEN' ~/.openclaw/.env"</automated>
  </verify>
  <done>All 3 env vars (GEMINI_API_KEY, TRANSCRIPT_API_KEY, APIFY_API_TOKEN) exist in ~/.openclaw/.env. Placeholders used for any keys the user hasn't provided yet. openclaw.json skill entries are correct.</done>
</task>

<task type="auto">
  <name>Task 2: Restart gateway and verify skill recognition</name>
  <files>none (service restart, no file modifications)</files>
  <action>
SSH to EC2 and restart the gateway, then verify:

1. **Restart the gateway service:**
   ```bash
   systemctl --user restart openclaw-gateway
   ```
   Wait 5 seconds for startup.

2. **Check service status:**
   ```bash
   systemctl --user status openclaw-gateway
   ```
   Must show "active (running)".

3. **Run doctor to check skill eligibility:**
   ```bash
   /home/ubuntu/.npm-global/bin/openclaw doctor
   ```
   Review output for:
   - All 5 skills should appear in the skills section
   - Skills with missing binaries (e.g., blogwatcher if deferred) will show as "blocked" or "ineligible" -- this is expected and acceptable
   - Skills with placeholder env vars will show warnings -- expected

4. **Capture the full doctor output** for the summary.

5. **Note any warnings/errors** about:
   - Sandbox binary access (binaries on host may not be available in Docker sandbox)
   - Missing API keys (expected for TRANSCRIPT_API_KEY and APIFY_API_TOKEN if user hasn't provided them)
   - Skill version mismatches

IMPORTANT: Gateway restart clears DM sessions. The user will need to DM Bob first to re-establish sessions after restart.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "systemctl --user is-active openclaw-gateway && /home/ubuntu/.npm-global/bin/openclaw doctor 2>&1 | head -50"</automated>
  </verify>
  <done>Gateway is running. Doctor output captured showing skill status for all 5 installed skills. Any eligibility issues are documented with clear reasons.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: User provides external API keys</name>
  <files>~/.openclaw/.env</files>
  <action>
All 5 ClawhHub skills installed, CLI dependencies set up, gateway restarted with skills loaded. Two skills need external API keys to function:

1. **youtube-full** -- needs TRANSCRIPT_API_KEY from https://transcriptapi.com (free: 100 credits/month)
2. **real-estate-lead-machine** -- needs APIFY_API_TOKEN from https://apify.com (free: $5/month credits)

The other 3 skills (nano-banana-pro, summarize, blogwatcher) use existing keys or no keys.

User action needed:
1. Check doctor output from Task 2 for any unexpected issues
2. If you have TranscriptAPI and Apify accounts, SSH to EC2 and replace placeholders in ~/.openclaw/.env with real keys, then restart gateway
3. If you don't have these accounts yet, the skills are installed but won't be fully functional until keys are added
4. DM Bob on Slack to re-establish session (gateway restart clears sessions)
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/clawdbot-key.pem ubuntu@100.72.143.9 "grep -v NEEDS_USER_INPUT ~/.openclaw/.env | grep -c 'TRANSCRIPT_API_KEY\|APIFY_API_TOKEN'"</automated>
  </verify>
  <done>User has reviewed skill installation, provided any needed API keys (or acknowledged placeholders), and confirmed the gateway is running with skills loaded.</done>
</task>

</tasks>

<verification>
- `systemctl --user is-active openclaw-gateway` returns "active"
- `openclaw doctor` shows all 5 skills recognized (eligible or with documented reasons for ineligibility)
- `grep -c 'TRANSCRIPT_API_KEY\|APIFY_API_TOKEN' ~/.openclaw/.env` returns 2
- No critical errors in gateway journal: `journalctl --user -u openclaw-gateway --since "5 min ago" --no-pager | grep -i error`
</verification>

<success_criteria>
Gateway is running with all 5 skills loaded. Environment variables are configured (with placeholders for any keys the user hasn't provided). Doctor output is clean (no unexpected criticals). User has been informed of any external accounts/keys needed.
</success_criteria>

<output>
After completion, create `.planning/phases/49-install-top-5-clawhhub-skills-on-ec2/49-02-SUMMARY.md`
</output>
